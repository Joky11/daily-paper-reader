---
title: JND-Guided Neural Watermarking with Spatial Transformer Decoding for Screen-Capture Robustness
title_zh: 基于JND引导的神经水印与空间变换器解码的屏摄鲁棒性方法
authors: "Jiayi Qin, Jingwei Li, Chuan Wu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.26766v1"
tags: ["query:q3"]
score: 10.0
evidence: 用于屏幕拍摄鲁棒性和图像保护的神经水印技术
tldr: 本文提出一种基于JND感知引导的神经水印方法，结合空间变换解码器以增强屏幕拍摄场景下的鲁棒性。该方法通过仿真真实屏摄噪声、引入人眼感知约束及自动定位模块，实现水印的高保真嵌入和可靠提取。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26766v1/fig-001.webp\", \"caption\": \"Table 3: Key training hyperparameters and their values.\", \"page\": 11, \"index\": 1, \"width\": 422, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2603.26766v1/fig-002.webp\", \"caption\": \"Table 1: Architecture of the encoder network. k: kernel size; s: stride.\", \"page\": 7, \"index\": 2, \"width\": 478, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2603.26766v1/fig-003.webp\", \"caption\": \"Table 2: Architecture of the decoder network. k: kernel size; s: stride. “–” indicates nonconvolutional layers.\", \"page\": 7, \"index\": 3, \"width\": 468, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2603.26766v1/fig-004.webp\", \"caption\": \"Table 7: Anti-cropping localization results (BER in %).\", \"page\": 12, \"index\": 4, \"width\": 521, \"height\": 319}]"
motivation: 由于屏幕拍摄引入复杂退化，传统水印方法难以兼顾视觉质量与水印可提取性。
method: 本文构建端到端网络，包含噪声模拟层、JND感知损失及自动定位解码模块，联合优化嵌入与提取过程。
result: 实验结果显示，本方法在30.94 dB PSNR和0.94 SSIM下可可靠嵌入127位水印信息。
conclusion: 该方法在保证图像质量的同时显著提升了水印的屏摄鲁棒性和自动化解码能力。
---

## 摘要
针对屏摄鲁棒性的水印技术旨在将可提取信息以不可察觉的方式嵌入宿主图像，使得水印能够在屏幕显示与摄像机重新拍摄所带来的复杂失真管道中依然存活。然而，在保持较高提取精度的同时维持令人满意的视觉质量仍是一项开放的挑战，主要原因在于屏摄信道会引入严重且交织的退化效应，包括摩尔纹、色域偏移、透视畸变以及传感器噪声。本文提出了一种端到端的深度学习框架，通过联合优化水印嵌入与提取过程，以增强对屏摄的鲁棒性。该框架包含三项核心创新：（i）一个综合噪声模拟层，能够真实建模屏摄过程中的各种失真——特别包括一个基于物理原理的摩尔纹生成器——从而使网络通过对抗训练学习到对全谱捕获信道噪声的鲁棒表示；（ii）一个基于可觉察失真阈（Just Noticeable Distortion, JND）的感知损失函数，可自适应调节水印嵌入强度，通过监督JND系数图与水印残差间的感知差异，将水印能量集中于感知不敏感区域，以最大化视觉质量；（iii）两个互补的自动定位模块——基于语义分割的前景提取器用于拍摄图像校正，以及对称噪声模板机制用于抗裁剪区域恢复——使系统能够在真实部署条件下实现全自动水印解码。大量实验结果表明，该方法在嵌入127比特负载的情况下，水印图像的平均PSNR达30.94 dB，SSIM达0.94。

## Abstract
Screen-shooting robust watermarking aims to imperceptibly embed extractable information into host images such that the watermark survives the complex distortion pipeline of screen display and camera recapture. However, achieving high extraction accuracy while maintaining satisfactory visual quality remains an open challenge, primarily because the screen-shooting channel introduces severe and entangled degradations including Moiré patterns, color-gamut shifts, perspective warping, and sensor noise. In this paper, we present an end-to-end deep learning framework that jointly optimizes watermark embedding and extraction for screen-shooting robustness. Our framework incorporates three key innovations: (i) a comprehensive noise simulation layer that faithfully models realistic screen-shooting distortions -- notably including a physically-motivated Moiré pattern generator -- enabling the network to learn robust representations against the full spectrum of capture-channel noise through adversarial training; (ii) a Just Noticeable Distortion (JND) perceptual loss function that adaptively modulates watermark embedding strength by supervising the perceptual discrepancy between the JND coefficient map and the watermark residual, thereby concentrating watermark energy in perceptually insensitive regions to maximize visual quality; and (iii) two complementary automatic localization modules -- a semantic-segmentation-based foreground extractor for captured image rectification and a symmetric noise template mechanism for anti-cropping region recovery -- that enable fully automated watermark decoding under realistic deployment conditions. Extensive experiments demonstrate that our method achieves an average PSNR of 30.94~dB and SSIM of 0.94 on watermarked images while embedding 127-bit payloads.

---

## 论文详细总结（自动生成）

# 《基于JND引导的神经水印与空间变换器解码的屏摄鲁棒性方法》论文总结

---

## 一、核心问题与研究背景

- **研究动机**：随着高分辨率显示器与智能设备普及，图像在“屏幕显示—摄像机重拍”这一物理传输过程中会引入复杂的退化（如摩尔纹、色域变化、透视畸变、噪声），导致传统数字域水印算法在屏摄场景下失效。
- **核心问题**：如何设计一种具备**高鲁棒性**、**高视觉质量**、且**可自动解码**的水印系统，使其在真实屏摄场景中仍能提取出嵌入信息。
- **研究意义**：该问题直接关系到版权追溯、内容防伪及多媒体可信认证等应用，是视觉安全与内容保护的重要方向。

---

## 二、方法与技术实现

### 1. 总体框架

- 建立一个 **端到端深度神经网络**，包括四个主要模块：
  1. **水印嵌入编码器（Encoder）**
  2. **噪声模拟层（Noise Simulation Layer）**
  3. **带空间变换解码器（STN-based Decoder）**
  4. **自动定位模块（Localization Modules）**

整体流程为：  
原图像 + 水印 → 编码器嵌入 → 模拟真实屏摄失真 → 解码器提取水印。

### 2. 编码器设计

- 基于 **U-Net 架构**，输入为宿主图像与二进制水印（共 6 通道），输出为 2 通道残差信号。
- 残差仅加到 **G、B 通道**，保留 **R 通道** 以嵌入冗余对称模板用于抗裁剪。
- 网络通过多尺度跳接（skip connections）在嵌入时兼顾细节与结构信息。

### 3. 解码器结构

- 两级设计：
  1. **Spatial Transformer Network (STN)**：学习仿射变换参数，实现几何校正（抵御旋转、缩放、平移、倾斜失真）。
  2. **卷积 + 全连接提取子网络**：将校正后图像映射到 127 位水印比特输出（Sigmoid 概率）。

### 4. 损失函数体系

综合四类损失，共同优化鲁棒性与视觉质量：

| Loss | 含义 | 功能作用 |
|------|------|----------|
| ℓ₁：MSE | 图像重建损失 | 提高 PSNR，限制像素偏差 |
| ℓ₂：LPIPS | 感知相似度 | 确保与人类视觉一致的质量 |
| ℓ₃：BCE | 信息交叉熵 | 提高水印提取正确率 |
| ℓ₄：JND 引导损失 | 感知约束 | 使残差信号集中于人眼不敏感区域 |

> 总体目标： L = λ₁ℓ₁ + λ₂ℓ₂ + λ₃ℓ₃ + λ₄ℓ₄。

### 5. 屏摄噪声仿真模块

构造综合的物理真实性能失真模型，关键模拟包括：

- **色域与亮度偏移**（对比度、亮度扰动）
- **饱和度变化**
- **高斯噪声**
- **运动模糊与离焦模糊**
- **摩尔纹生成（核心创新）**：
  1. LCD 子像素重采样  
  2. 随机投影 + 模糊  
  3. Bayer CFA 上采样与逆变换  
  → 逼真还原显示与传感器干涉的摩尔纹结构。

### 6. 自动定位与抗裁剪模块

- **前景定位模块**：
  - 结合传统边缘检测 + 深度语义分割（DeepLab V3+），能自动定位并校正屏摄图片中被显示的水印区域。
- **抗裁剪机制**：
  - 将图像划分为四子区域分别嵌入同一水印，同时在 red 通道嵌入对称噪声模板。
  - 通过对称性检测定位裁剪后的有效区域，可在保留 ≥25% 图像时成功恢复水印。

---

## 三、实验设计

- **训练数据**：
  - 使用 *Flickr1M* 数据集前 100,000 张图像进行训练；
  - 测试使用 *Flickr2K* 前 2,000 张图像；统一分辨率：512×512。
- **屏摄场景评测**：
  - 不同显示设备（显示器、笔记本、平板、电视）；
  - 不同拍摄设备（智能手机 Redmi K30 Pro、Sony A6100）；
  - 多角度（-75° 至 +75°）、多距离（40–200 cm）。
- **对比算法**：
  - 传统方法：Nakamura (2004)、Pramila (2012)；
  - 深度方法：SSRW (2019)、DTW (2021)、TERA (2022)。
- **评价指标**：
  - *视觉质量*：PSNR, SSIM, LPIPS；
  - *鲁棒性*：Bit Error Rate (BER)；
  - *效率*：每张图像处理时间（毫秒级）。

---

## 四、算力与训练资源

- 文中明确说明：
  - GPU：NVIDIA **RTX 2070 Super**；
  - CPU：AMD Ryzen 5 2600X；
  - 内存 32 GB；
  - 训练迭代 175,000 次；
  - 学习率 10⁻⁴，批量大小 16；
  - 单张图像处理时间约 0.064 秒（实时可用）。
- 未说明 GPU 数量及总训练时长（仅推测为单卡训练）。

---

## 五、实验数量与充分性

- **消融实验**：
  - JND损失有效性（比较无JND、有JND两种架构）；
  - 摩尔纹仿真模块影响；
  - 定位与抗裁剪模块对性能提升的贡献。
- **鲁棒性评测**：
  - 不同角度、距离、设备组合共几十组；
  - 考察在高噪声与裁剪条件下 BER 表现。
- **对比实验**：
  - 与 5 种代表性 SOTA 方法在多指标上对比。
- **充分性评价**：
  - 实验覆盖主流屏摄失真类型与典型设备；
  - 消融和对比实验较全面，具备较高客观性。

---

## 六、主要结论与发现

- 能在嵌入 **127-bit** 水印时保持 **PSNR=30.94 dB、SSIM=0.94、LPIPS=0.05**；
- 屏摄下 BER **约1%–3%**，显著低于对比方法；
- 抗裁剪性能优异，保留 ≥25% 区域仍可恢复；
- 总处理时间 <0.1 s/张；
- 证明引入 **JND 感知损失 + 物理仿真噪声** 能有效兼顾**感知质量**与**物理鲁棒性**。

---

## 七、优点与创新

- ✅ **物理真实的摩尔纹建模**：首次引入LCD子像素级别仿真以增强屏摄鲁棒性。  
- ✅ **JND感知损失设计**：结合视觉可觉察阈模型与LPIPS感知度量，显著改善主客观质量。  
- ✅ **自动化解码体系**：无需人工裁剪或标定，实现真正“拍照即解码”。  
- ✅ **多重鲁棒性方案**：同时应对几何畸变、噪声偏差与裁剪破坏。  
- ✅ **实验全面、结果可复现**：消融与对比充分，设备配置明确。  

---

## 八、不足与局限

- ⚠ **场景局限**：主要验证于常见屏幕与光照条件，未涵盖极端亮度或动态环境。  
- ⚠ **算力信息欠详**：未报告训练时长、显存消耗等细节。  
- ⚠ **仅支持静态图像**：对视频或流媒体帧间一致性未作探讨。  
- ⚠ **嵌入容量与质量权衡**：在更高位容量(>127bit)下的性能衰减未进一步分析。  
- ⚠ **安全性维度未验证**：如针对对抗性攻击或故意扰动的防御尚未评估。  

---

**（完）**
