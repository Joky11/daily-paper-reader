---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一种可实现精确生成时间恢复的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于精确生成时间恢复和真伪验证的水印技术
tldr: 论文提出TimeMark可信时间水印框架，应对AIGC版权争议与伪造风险。该方法通过引入时间相关的加密密钥和双阶段编码机制，实现无统计特征的水印嵌入与精确生成时间恢复。实验和理论分析显示其可靠性和防伪性均达到司法证据要求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有基于统计信号的AIGC水印方法可靠性差且易被伪造，无法满足司法证据需求。
method: 采用密码学技术生成与时间关联的密钥并结合两阶段编码与纠错机制实现无统计特征的精确水印。
result: "理论与实验均验证该框架可100%准确恢复生成时间，具备防伪与高可信度。"
conclusion: TimeMark框架实现了可信时间水印的理论完美恢复，为AIGC知识产权争议提供可靠司法证据。
---

## 摘要
大型语言模型（LLMs）在文本生成中的广泛应用，引发了关于知识产权纠纷的日益增多的担忧。水印技术通过将元信息嵌入人工智能生成内容（AIGC），有潜力作为司法证据。然而，现有方法依赖于词元分布中的统计信号，导致检测结果本质上具有概率性，并在多比特编码（例如时间戳）中可靠性下降。此外，这类方法会引入可检测的统计模式，使其易受伪造攻击，同时允许模型提供者伪造任意水印。为了解决这些问题，我们提出了“可信水印”（trustworthy watermark）的概念，它能够在抵抗用户端统计攻击和提供方伪造的同时，实现 100% 识别精度的可靠恢复。我们聚焦于可作为司法证据使用的可信时间水印。我们的框架融合了密码学技术，在监管监督下将时间信息编码进时间相关的秘密密钥中，以防止任意时间戳的伪造。水印载荷与时间解耦，并针对每个实例生成随机、非存储的比特序列，从而消除统计模式。为保证可验证性，我们设计了一个两阶段编码机制，并结合纠错码，使生成时间能够以理论上完美的精度可靠恢复。理论分析与实验结果均表明，我们的框架满足司法证据的可靠性要求，为未来与 AIGC 相关的知识产权纠纷提供了可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# 《TimeMark：一种可实现精确生成时间恢复的可信时间水印框架》论文总结

---

## 一、核心问题与研究背景

- **研究动机**  
  随着大型语言模型（LLMs）在文本生成中的应用增多，AI 生成内容（AIGC）引发了日益严重的**知识产权归属与抄袭争议**。  
  传统数字证据体系可依赖时间戳或版本历史来界定“创作时间”，但 LLM 生成文本缺乏生成时间的可信记录。这使得在司法场景中，**难以证明作品的时间优先性与原创性**。

- **问题定义**  
  当前主流的 LLM 水印方案通常通过改变词元采样分布来嵌入统计信号。  
  然而，这些方案存在三大问题：
  1. **检测本质上是概率性的**，无法保证 100% 精确识别；
  2. **多比特信息编码（如时间戳）会严重降低恢复准确率**；
  3. 水印结构引入统计模式，既容易被用户推测伪造，也允许**模型提供者伪造任意时间戳**。

- **研究目标**  
  作者提出“**可信时间水印（trustworthy time watermark）**”框架，旨在在司法标准下，实现：
  1. 完全准确的生成时间恢复；
  2. 对抗用户端伪造与服务提供方伪造；
  3. 满足司法证据的真实性、可验证性与防篡改性要求。

---

## 二、方法论：TimeMark 框架设计

- **核心思想**
  TimeMark 不直接将时间作为水印载荷，而是将时间信息**嵌入时间相关的密钥（Key）**中。  
  每次生成文本时，载荷是一个随机生成、非存储的比特序列，与时间解耦。  
  此设计保证水印无统计模式，可防统计攻击，同时防止服务方伪造历史时间戳。

- **关键技术模块**
  1. **时间相关密钥生成**  
     - 时间被离散化为窗口（如每分钟一个时间段），每个时间段生成一个密钥：  
       \( K_t = H(K_{t-1}) \)，H 为单向哈希函数（如 SHA-256）。  
     - 密钥存储在**硬件安全模块（HSM）**中，受监管访问。服务提供方无法回溯旧密钥。
  2. **随机载荷生成与纠错编码**  
     - 每次生成随机比特序列 \( R \in \{0,1\}^m \)，并经 **BCH 纠错编码** 得到 \( P = ECC(R) \)。  
     - 提高码字间冗余度，确保即便部分位识别错误，也可正确恢复。
  3. **双阶段编码机制**
     - **阶段Ⅰ**：前半部分 token 的水印嵌入由 \( K_t, R, prefix \) 共同生成伪随机种子；  
       通过偏置 logits 实现绿色列表（greenlist）和红色列表（redlist）之间的采样偏好。  
     - **阶段Ⅱ**：后续 token 仅使用时间密钥 \( K_t \)，不包含 R，供验证使用。
  4. **解码与验证流程**
     - **步骤一**：基于候选时间密钥 \( K_t \) 对阶段Ⅱ解码，获得 ˆR；
     - **步骤二**：利用 ˆR 与 \( K_t \) 重构阶段Ⅰ的 greenlist，计算匹配率 Score；若 Score ≥ 阈值 ϕ（如 0.65），则确认生成时间为对应时段。

---

## 三、实验设计

- **实验设置**
  - 使用 **Qwen2.5-7B** 模型；
  - 构建 **20 个 prompt 组成的生成任务池**（含创意写作与报告生成场景）；
  - 每次生成两类文本：带水印与不带水印；
  - 共执行 **800 组文本生成及验证实验**。

- **验证策略**
  - 对每条生成文本，考虑**正确时间密钥及相邻 4 个密钥**（共 5 候选），模拟司法检验期内的时间不确定性；
  - 使用 TimeMark 的两阶段解码过程识别生成时间。

- **评价指标**
  - 正确生成时间识别率；
  - 非水印文本的误识别率；
  - 验证匹配得分（Score）。

---

## 四、资源与算力

- **算力信息**
  论文未明确说明使用的 **GPU 类型、数量或运行时间**；  
  仅说明模型采用 Qwen2.5-7B 推理生成，计算复杂度主要集中在 token 采样偏置与解码统计上。  
  因此可视为**轻量级实验**，非重训练型消耗。

---

## 五、实验数量与充分性

- **实验规模**：800 组对照试验（含有/无水印），对多个时间密钥候选验证。  
- **结果覆盖**：涉及多 prompt 类型和时间窗口，符合框架的主要应用场景。  
- **消融或对比**：未直接与其他水印方法（如 SynthID、StealthInk）做数值对比，而以**理论推导 + 正负样本识别实验**来论证可靠性。  
- **充分性分析**：  
  实验验证了准确识别率、误识率与理论推断一致性，但**缺乏跨模型泛化与抗编辑测试**，因此实验验证聚焦于“理论完美恢复”假设下的理想场景。

---

## 六、主要结论与发现

- **核心结果**
  | 项目 | 结果 |
  |------|------|
  | 正确生成时间识别率 | 100%（800/800） |
  | 非水印文本误识率 | 0% |
  | 水印文本平均验证分数 | 0.7699 |
  | 非水印文本平均验证分数 | 0.4993 |

- **理论分析**
  - BCH(63,10) 码可纠正最多 13 位错误；
  - 在 δ=2.5、α=5、L=945 条件下，成功恢复 R 的概率近似 1；
  - 误拒概率 ≈1.6×10⁻⁴⁴，误接收概率 ≈4.7×10⁻⁸；
  - 说明系统几乎可达**无统计攻击下的理论完美识别**。

- **总体结论**
  TimeMark 能以**100% 精确率恢复生成时间**；防止服务方伪造历史水印；并因随机化载荷消除统计可模仿特征，从理论上符合司法证据“真实性、可验证性、防篡改性”的五项要求。

---

## 七、优点与创新点

- **法律与技术结合**：首次系统化地将电子证据司法标准（真实性、可验证性、防篡改等）形式化为系统设计目标。
- **时间与载荷分离设计**：将时间绑定至不可逆密钥链，而非嵌入水印载荷，彻底阻断提供方伪造。
- **双阶段编码**：第二阶段实现独立可验证性，使得随机载荷可验证，从而消除“随机性导致无法校验”的矛盾。
- **理论完备分析**：提供概率计算与纠错编码公式，定量验证系统可靠性。
- **高准确率与防伪性**：实验与理论一致，满足司法场景高可信要求。

---

## 八、不足与局限

- **适用范围受限**：当前框架需较长文本（≥945 tokens）以保证编码冗余。
- **对编辑鲁棒性不足**：假设用户未修改原文，未测试 paraphrasing、剪裁、翻译等情况。
- **缺乏跨模型验证**：实验仅在单模型（Qwen2.5-7B）上进行，未覆盖多架构或微调模型。
- **计算与法律互操作**：HSM 监管及密钥管理的实际部署成本与可审计机制有待工程化研究。
- **无算力或对比指标**：未说明对比现有方法的性能差异或消融效果。

---

（完）
