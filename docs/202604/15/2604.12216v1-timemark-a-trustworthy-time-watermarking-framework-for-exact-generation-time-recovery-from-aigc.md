---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一种可精确恢复生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q8"]
score: 9.0
evidence: 用于AIGC验证和生成时间恢复的水印技术
tldr: 本文提出TimeMark框架，针对AI生成内容在知识产权争议中的司法取证需求，通过引入加密技术与时间依赖密钥实现可信时间水印，结合两阶段编码与纠错机制保障可靠恢复与防伪能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有AI生成内容水印方法可靠性不足，易受攻击且可被伪造，难以在司法取证中使用。
method: 提出使用加密和时间依赖秘钥的可信水印框架，采用双阶段编码和纠错码实现精确生成时间恢复。
result: "理论分析和实验表明，该框架能以100%准确率恢复生成时间并抵御伪造与统计攻击。"
conclusion: TimeMark框架满足司法证据的可靠性要求，为未来AIGC知识产权纠纷提供可行解决方案。
---

## 摘要
大型语言模型（Large Language Models, LLMs）在文本生成中的广泛应用引发了日益增多的知识产权争议。通过将元信息嵌入AI生成内容（AIGC）中的水印技术，有潜力作为司法证据。然而，现有方法依赖于 token 分布的统计信号，导致检测结果具有固有的概率性，并在多比特编码（例如时间戳）情况下可靠性较低。此外，这类方法会引入可检测的统计模式，使其易受伪造攻击，并可能被模型提供方任意伪造水印。为解决这些问题，我们提出了“可信水印”（trustworthy watermark）的概念，该方法在实现100%识别准确率的可靠恢复的同时，能够抵抗用户侧的统计攻击与提供方的伪造攻击。我们重点研究可用于司法取证的可信时间水印。我们的框架融合密码学技术，在监管监督下，将时间信息编码进依赖时间的秘密密钥中，从而防止任意时间戳伪造。水印载荷与时间解耦，并针对每个实例生成随机且不存储的比特序列，从而消除统计特征。为了保证可验证性，我们设计了一个结合纠错码的双阶段编码机制，使得生成时间能够以理论上的完美精度被可靠恢复。理论分析和实验结果表明，我们的框架满足司法证据的可靠性要求，并为未来涉及AIGC的知识产权争议提供了切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# TimeMark：一种可精确恢复生成时间的可信时间水印框架 —— 深度总结

## 一、核心问题与研究背景

- **核心问题**：随着生成式人工智能（AIGC）的普及，AI生成内容的**版权归属与创作时间**成为潜在的司法争议焦点。  
  - 传统数字签名、区块链或日志方法往往无法记录**内容生成时刻**，仅能标定发布或存储时间。  
  - 现有LLM水印（如Kirchenbauer等, 2023）多基于token分布偏差的统计性信号，检测结果不确定且易受**伪造与反向攻击**。  

- **核心挑战**：
  1. 生成时间需能**100%准确恢复**，不能存在水印解码误差。  
  2. 方法需防止**提供方伪造时间戳（provider-side forgery）**与**用户侧统计攻击**。  
  3. 需满足**司法证据标准**：真实性、完整性、可验证性及常规生成性。  

- **目标**：构建一个可被法院信赖的**“可信时间水印”（Trustworthy Time Watermark）**框架，使每个AIGC作品可溯源其实际生成时间。

---

## 二、方法论与技术框架

### 1. 核心思想
- 将“时间”信息不再直接编码入文本，而是通过**时间依赖的密钥（time-dependent key）**间接绑定。
- 使用随机且不存储的比特序列作为水印载荷，**去除稳定统计模式**，从根本上防止统计分析攻击。
- 在监管监督下借助**硬件安全模块（HSM）**管理密钥，使模型提供者无法回溯或伪造历史时间戳。
- 采用**双阶段编码机制**与**纠错码（BCH code）**实现高可靠性恢复。

### 2. 算法与流程结构（文字描述）
#### （1）密钥与时间绑定
- 时间被划分为离散窗口 \( t \)，每一窗口生成密钥：
  \[
  K_t = H(K_{t-1})
  \]
  由单向哈希函数生成（例如SHA-256），保证无法推回历史密钥。  
- HSM负责保存历史密钥，防止服务商篡改。

#### （2）载荷生成与嵌入
- 随机生成短比特串 \( R \in \{0,1\}^m \)，经过BCH编码为码字 \( P = ECC(R) \)。
- 位置分配函数 \( A(i) \) 确定每个token对应的payload位。
- 两阶段编码：
  - **阶段I（前半部分）**：种子 \( S^1_i = PRF(K_t, R, h(prefix_i)) \)
  - **阶段II（后半部分）**：种子 \( S^2_i = PRF(K_t, h(prefix_i)) \)
- 根据种子决定词表排列及greenlist/redlist分区，并通过logit偏置（如δ=2.5）嵌入水印。

#### （3）解码与验证
- 候选时间集合 \( T = \{t_1,...,t_n\} \)。  
- **步骤1**：用每个 \( K_t \) 解码BCH码还原 \( \hat{R} \)，重建payload。
- **步骤2**：验证 \( \hat{R} \) 与对应 \( K_t \)，计算匹配率：
  \[
  Score(K_t, \hat{R}) = \frac{1}{\alpha|P|}\sum Match(i)
  \]
  高于阈值ϕ（如0.65）者即确认生成时间。

---

## 三、实验设计

- **模型环境**：Qwen2.5-7B。  
- **任务**：从20个创意与报告写作提示（prompt pool）中随机选取，生成带水印与无水印两种文本。  
- **总样本**：800次生成测试（含等量有/无水印文本）。  
- **解码验证**：每条文本在正确时间键及前后邻近时间键（共5个候选）间比对。  
- **指标**：
  - 正确识别的生成时间数量
  - 误报率（非水印文本误识别）
  - Step 2 验证得分均值  

- **基准比较**：与标准LLM生成（无水印）做对照，但未与其他具体水印算法（如SynthID、Stealthink等）直接对比。  
  （理由：目标是司法级可信度验证，而非检测精度benchmark。）

---

## 四、资源与算力使用

- **论文未说明具体算力配置**（例如GPU种类、数量或运行时长）。  
- 仅提及运算负载基于Qwen2.5-7B，未进行大规模分布式训练，因此应为轻量推理级实验。

---

## 五、实验数量与充分性

- 实验共 **800 组**（400带水印，400无水印）。  
- 每组均进行5个候选key验证 → 实际解码尝试约4000次以上。  
- 附有理论推导支持误差容忍度（BCH纠错能力、匹配率统计分布）。  
- 但：
  - **未报告跨模型验证**（如GPT、LLama系列）。  
  - **无用户编辑/攻击场景实验**。  

→ 实验充分证明了**理论正确性与实现可行性**，但外部鲁棒性尚待长期评估。

---

## 六、主要结论与发现

- **核心结果**：
  - 生成时间恢复准确率：**100%（800/800）**。  
  - 非水印文本误识别率：**0%**。  
  - 平均验证得分：有水印 0.77，对照 0.50。  
  - 理论上可实现**完美识别准确率与极低伪造概率（10⁻⁸量级）**。  

- **发现**：
  - 双阶段验证在正确key下几乎无假阴性（概率10⁻⁴⁴）。  
  - 在错误key下假阳性率极低，验证门槛设置合理（ϕ=0.65）。  
  - 可作为司法证据满足真实性、可验证性、常规生成性等标准。

---

## 七、方法优点与创新点

- **创新解耦设计**：将“时间”与“水印载荷”解耦；核心时间信息受密钥控制而非直接编码，彻底避免provider伪造。  
- **加密安全保障**：利用**HSM+哈希链**确保时间不可逆与监管追踪。  
- **双阶段验证机制**：实现载荷可检与密钥可证的闭环验证。  
- **纠错增强**：BCH(63,10)提供多位冗余纠错，使随机误差可容忍达13位。  
- **统计防御能力强**：随机载荷消除样本间稳定模式，抵御用户侧的大规模统计学习攻击。  

---

## 八、不足与局限

- **文本长度限制**：需较长文本（945 tokens以上）才能保证足够信号长度；短文本不适用。  
- **无编辑鲁棒性**：未验证对人工修改、重写、翻译等操作的稳健性。  
- **模型单一性**：实验仅基于Qwen2.5-7B；通用性未验证。  
- **缺少横向benchmark比较**：未与当前多比特水印系统进行实测对比。  
- **法律与实现依赖**：需HSM监管支持及时间同步服务，部署成本较高。  

---

**（完）**
