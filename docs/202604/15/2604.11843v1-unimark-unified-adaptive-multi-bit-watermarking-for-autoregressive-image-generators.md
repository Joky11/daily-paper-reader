---
title: "UniMark: Unified Adaptive Multi-bit Watermarking for Autoregressive Image Generators"
title_zh: UniMark：自回归图像生成器的统一自适应多比特水印
authors: "Yigit Yilmaz, Elena Petrova, Mehmet Kaya, Lucia Rossi, Amir Rahman"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.11843v1"
tags: ["query:q3"]
score: 9.5
evidence: 用于自回归图像生成器的统一多比特水印，旨在保护所有权
tldr: 本研究提出UniMark框架，用于自回归图像生成模型的隐形多比特水印嵌入，克服现有方法仅支持零比特验证、固定码本易受攻击及架构专属等局限，实现无训练、跨模型、可自适应的高鲁棒性水印。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11843v1/fig-001.webp\", \"caption\": \"Figure 4: Security analysis: (a) green ratio distributions under correct/wrong keys; (b) forgery success rate vs. partition exposure fraction.\", \"page\": 11, \"index\": 1, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-002.webp\", \"caption\": \"Figure 5: Efficiency analysis: (a) absolute time comparison (log scale); (b) relative overhead percentage.\", \"page\": 11, \"index\": 2, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-003.webp\", \"caption\": \"Figure 2: Radar chart comparing robustness (TPR@FPR=1%) across six attack types on LlamaGen.\", \"page\": 10, \"index\": 3, \"width\": 392, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-004.webp\", \"caption\": \"Figure 3: Parameter sensitivity analysis: effect of green ratio γ on (a) FID, (b) TPR@FPR=1%, and (c) 32-bit extraction accuracy.\", \"page\": 10, \"index\": 4, \"width\": 822, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-005.webp\", \"caption\": \"Table 1: Image quality comparison across AR models. ∆FID denotes the FID increase compared to unwatermarked generation.\", \"page\": 8, \"index\": 5, \"width\": 636, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-006.webp\", \"caption\": \"Figure 6: Attack strength analysis: (a) JPEG quality, (b) Gaussian noise σ, (c) crop ratio vs. detection and extraction performance.\", \"page\": 12, \"index\": 6, \"width\": 821, \"height\": 220}]"
motivation: 现有自回归图像水印方法无法实现多比特信息传输且易被攻击，亟需安全通用的新方案。
method: UniMark结合自适应语义分组、块级多比特编码和统一令牌替换接口，以动态、安全地植入多比特水印。
result: 实验结果显示，UniMark在图像质量、检测准确率及多比特信息提取方面均达成最新性能，并对多种攻击保持稳健。
conclusion: UniMark在多种模型上均展现出高质量、高准确率及强鲁棒性，证明了其通用性与实用价值。
---

## 摘要
针对自回归（Autoregressive，AR）图像生成中的隐形水印技术，近年来作为保护图像所有权和追踪AI生成内容的方法受到广泛关注。然而，现有方法存在三大局限：（1）仅嵌入用于二元验证的零比特水印，缺乏传递多比特信息的能力；（2）依赖静态的码本划分策略，一旦划分方式被泄露，容易遭受安全攻击；（3）针对特定的AR架构设计，难以泛化至多样的AR范式。为此，我们提出了\method{}，一种无需训练的统一水印框架，能够解决上述三大问题。\method{} 包含三个核心组件：\textbf{自适应语义分组（Adaptive Semantic Grouping, ASG)}，可依据语义相似性和密钥动态划分码本条目，从而在保持图像质量的同时增强安全性；\textbf{块级多比特编码（Block-wise Multi-bit Encoding, BME)}，将token序列划分为多个块，并通过纠错编码在不同块中嵌入多比特信息，以实现可靠的信息传输；以及\textbf{统一token替换接口（Unified Token-Replacement Interface, UTRI)}，该接口抽象化水印嵌入过程，以同时支持下一token预测（如LlamaGen）与下一尺度预测（如VAR）两种范式。我们对检测误差率和嵌入容量进行了理论分析。在三个AR模型上的大量实验表明，\method{}在图像质量（FID）、水印检测精度和多比特消息提取方面均达到了当前最优性能，同时在应对裁剪、JPEG压缩、高斯噪声、模糊、颜色偏移和随机擦除攻击时保持了较强的鲁棒性。

## Abstract
Invisible watermarking for autoregressive (AR) image generation has recently gained attention as a means of protecting image ownership and tracing AI-generated content. However, existing approaches suffer from three key limitations: (1) they embed only zero-bit watermarks for binary verification, lacking the ability to convey multi-bit messages; (2) they rely on static codebook partitioning strategies that are vulnerable to security attacks once the partition is exposed; and (3) they are designed for specific AR architectures, failing to generalize across diverse AR paradigms. We propose \method{}, a training-free, unified watermarking framework for autoregressive image generators that addresses all three limitations. \method{} introduces three core components: \textbf{Adaptive Semantic Grouping (ASG)}, which dynamically partitions codebook entries based on semantic similarity and a secret key, ensuring both image quality preservation and security; \textbf{Block-wise Multi-bit Encoding (BME)}, which divides the token sequence into blocks and encodes different bits across blocks with error-correcting codes for reliable message transmission; and \textbf{a Unified Token-Replacement Interface (UTRI)} that abstracts the watermark embedding process to support both next-token prediction (e.g., LlamaGen) and next-scale prediction (e.g., VAR) paradigms. We provide theoretical analysis on detection error rates and embedding capacity. Extensive experiments on three AR models demonstrate that \method{} achieves state-of-the-art performance in image quality (FID), watermark detection accuracy, and multi-bit message extraction, while maintaining robustness against cropping, JPEG compression, Gaussian noise, blur, color jitter, and random erasing attacks.

---

## 论文详细总结（自动生成）

# UniMark：自回归图像生成器的统一自适应多比特水印 — 综合总结

## 一、研究背景与核心问题

- **研究动机**：随着自回归（Autoregressive, AR）图像生成模型（如LlamaGen、VAR）的发展，其生成的高质量AI图像在版权保护与内容可追溯性方面带来了新的挑战。传统隐形水印方法主要集中于扩散模型，而针对AR生成的方案仍处于初级阶段。  
- **问题根源**：现有AR水印方法存在三大限制：
  1. **仅支持零比特验证**：只能检查图像是否带水印，无法传输多比特身份信息。
  2. **静态码本划分**：水印嵌入依赖固定红绿列表，一旦被泄露易被攻击。
  3. **架构特定性强**：针对特定模型设计，无法跨不同的AR生成范式（如next-token或next-scale）。
- **研究意义**：论文旨在提出一种统一、模型无关且无需再训练的水印机制，实现**可嵌入多比特信息的隐形水印**，在安全性、可移植性和鲁棒性上全面提升。

---

## 二、方法论：UniMark 框架设计

### 1. 核心思想
UniMark（Unified Adaptive Multi-bit Watermarking）是一种**无训练（training-free）**、**跨架构**的水印嵌入框架。其核心思路是在生成token序列阶段动态调整token选择，从而隐式编码多比特信息，并在不损害图像质量的前提下保持高安全性与鲁棒性。

### 2. 三大关键模块

- **（1）自适应语义分组（ASG, Adaptive Semantic Grouping）**
  - 按token语义相似度对码本进行聚类。
  - 结合密钥 κ 和哈希函数 H(κ∥i) 动态生成每个位置的分组（绿组 Gᵢ 和红组 Rᵢ）。
  - 将不在绿组的token替换为语义最相近的绿组token，保证视觉一致性。
  - **核心优势**：每个token位置的划分均与位置和密钥相关，杜绝了静态分割导致的可破解性。

- **（2）块级多比特编码（BME, Block-wise Multi-bit Encoding）**
  - 将token序列分为多个块，利用纠错编码（BCH codes）在每块嵌入一个比特。
  - “1”比特块偏向绿组，“0”比特块偏向红组。
  - 在检测阶段计算各块中绿token比例，对比阈值后经BCH译码提取消息。
  - **具备多比特承载与误差恢复机制**。

- **（3）统一令牌替换接口（UTRI, Unified Token-Replacement Interface）**
  - 将具体生成模型抽象为三个通用操作：
    1. 生成token序列（GET TOKEN SEQUENCE）；
    2. 执行语义替换（REPLACE TOKENS）；
    3. 再解码还原图像（DECODE IMAGE）。
  - 支持next-token与next-scale两类AR模型，通过适配器无缝集成。

### 3. 理论分析
- **假阳率**：基于中心极限定理推导水印检测的显著性水平 α 满足  
  \( P_{FP} = P(Z > z_α) = α \)。  
- **嵌入容量**：公式给出可可靠嵌入的最大比特  
  \(B_{max} = \lfloor N / N_{min} \rfloor \cdot R_{BCH}\)，  
  其中 \(N_{min} = 4z^2_{α}/δ^2\)。容量与token序列长度 N 呈线性关系。

---

## 三、实验设计

### 1. 测试模型与场景
论文在三种代表性AR图像生成器上测试：
- **LlamaGen-XL**（next-token预测, N=256, K=16384）  
- **VAR-d20**（next-scale预测, N=680, K=4096）  
- **Open-MAGVIT2**（next-token预测, N=256, K=262144）

### 2. 对比基线方法
- IndexMark（Tong et al., 2025）：静态红绿列表、零比特。
- AR-Watermark（Jovanović et al., 2025）：LLM风格红绿嵌入。
- Tree-Ring（Wen et al., 2023）：用于扩散模型的水印方法（横向比较）。

### 3. 指标与攻击场景
- **指标**：  
  - 图像质量（FID ↓, PSNR ↑, SSIM ↑）  
  - 检测性能（TPR@FPR=1%）  
  - 信息提取准确率（Bit Accuracy ↑）
- **攻击类型**（6种）：JPEG压缩、Gaussian噪声、模糊、裁剪、颜色扰动、随机擦除。  
- **参数设置**：γ=0.5，B=32bit，BCH(63,36,5)码，显著性α=0.01；共生成5000张ImageNet验证集图片。

---

## 四、算力与资源

- 论文未显式说明**GPU型号、数量或运行时间**。
- 提到的开销仅指生成阶段额外耗时：
  - 嵌入阶段平均增加 **8–15毫秒/图像**，
  - 相对生成时间的额外开销约 **1.2–1.9%**，
  表明该方案计算代价极低。

---

## 五、实验数量与充分性

- **主实验**：3种模型 × 多种水印长度（16, 32, 48, 64 bit），共数十组结果。  
- **鲁棒性测试**：6类攻击 × 3模型。  
- **消融实验**：验证ASG、BME、UTRI、误差校正各组件贡献。  
- **参数敏感性分析**：γ从0.3至0.7的变化。  
- **容量分析、代码本规模实验、安全与效率分析**等附加实验亦包含。  
  → 实验覆盖广、设计系统、指标完整，对照公平，验证充分。

---

## 六、主要结论与发现

- **性能优越**：在FID、检测精度与多比特信息提取三方面均达SOTA。  
- **32-bit信息嵌入**准确率 >99%，在64-bit时仍保持>95%。  
- **鲁棒性强**：对JPEG压缩、裁剪、噪声等干扰均可保持>90%检测率。  
- **安全性高**：分组密钥依赖显著减少伪造成功率（<2%）。  
- **跨模型通用性**：UTRI实现了对不同AR范式的兼容。  
- **计算高效**：平均开销<2%。

---

## 七、优点与亮点

- **创新性设计**：首个同时实现“多比特编码 + 动态安全分组 + 跨架构支持”的AR水印框架。  
- **训练无依赖**：完全无需额外微调或训练阶段处理。  
- **安全可控**：分组哈希化与密钥化机制，强抵伪造攻击。  
- **理论严谨**：对假阳率及容量极限提供可解释数学推导。  
- **效率与质量兼顾**：几乎不损伤图像质量（ΔFID ≤ 0.1），开销极小。  
- **实验全面**：包含对不同模型、攻击、超参及组件的系统评测。

---

## 八、不足与局限

- **容量限制**：嵌入量受token长度与码本规模限制，暂时难以支持大规模信息传输。  
- **未验证视频或跨模态场景**：主要聚焦静态图像，未来需扩展到视频AR生成或多模态生成任务。  
- **攻击类型有限**：虽涵盖常见物理/图像失真攻击，但未测试对抗性再生成（adversarial regeneration）攻击。  
- **无算力细节**：论文未说明实验硬件或生成成本，影响重现实验的可比性。  
- **固定超参策略**：γ、τ等阈值虽经敏感性分析，但动态调参机制尚未探讨。

---

**（完）**
