---
title: "XMark: Reliable Multi-Bit Watermarking for LLM-Generated Texts"
title_zh: XMark：用于LLM生成文本的可靠多比特水印
authors: "Jiahao Xu, Rui Hu, Olivera Kotevska, Zikai Zhang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05242v1"
tags: ["query:q8"]
score: 9.0
evidence: 用于LLM生成文本的多比特水印技术，实现归属和追踪
tldr: 本文提出XMark，一种针对大语言模型生成文本的多比特水印方法，解决现有方案在文本长度限制与质量平衡上的难题。XMark利用低失真logit分布编码策略和优化解码器，大幅提升文本质量与解码准确度，在多项实验中超越现有技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05242v1/fig-001.webp\", \"caption\": \"Table 7: Performance comparison on text completion tasks with message length b = 16 under different numbers of available tokens T ∈ {150, 200, 250, 300} on the Essays and OpenGen datasets.\", \"page\": 15, \"index\": 1, \"width\": 814, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2604.05242v1/fig-002.webp\", \"caption\": \"Table 8: Performance comparison on text completion tasks with message length b = 32 under different numbers of available tokens T ∈ {150, 200, 250, 300} on the Essays and OpenGen datasets.\", \"page\": 15, \"index\": 2, \"width\": 814, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.05242v1/fig-003.webp\", \"caption\": \"Table 6: Notation table.\", \"page\": 13, \"index\": 3, \"width\": 446, \"height\": 839}, {\"url\": \"assets/figures/arxiv/2604.05242v1/fig-004.webp\", \"caption\": \"Table 11: Example of generated texts of XMARK.\", \"page\": 17, \"index\": 4, \"width\": 928, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2604.05242v1/fig-005.webp\", \"caption\": \"Table 10: Performance comparison of MPAC and XMARK on Qwen2.5-7B and LLaMA-3.1-8B models with message lengths b ∈ {8, 16} and number of available tokens T ∈ {150, 200, 250, 300}.\", \"page\": 16, \"index\": 5, \"width\": 879, \"height\": 265}]"
motivation: 现有多比特水印方法在大消息或短文本场景下的解码精度及性能受限。
method: XMark通过设计低失真logit分布的编码器与匹配的解码器，实现高质量文本下的稳健消息嵌入与提取。
result: 实验结果显示，XMark在多种下游任务中解码准确率明显提高，且文本自然度保持良好。
conclusion: XMark显著提升了多比特水印在LLM文本中的解码精度与文本质量，表现优于现有方法。
---

## 摘要
多比特水印技术已成为一种有前景的解决方案，用于在大型语言模型（LLM）生成的文本中嵌入不可察觉的二进制信息，从而实现对LLM的可靠归属和恶意使用的追踪。尽管近期取得了一定进展，现有方法仍存在关键局限：部分方法在处理大规模信息时计算成本不可接受，而另一些方法则在文本质量与解码准确率之间存在不良的权衡。此外，当生成文本中的token数量有限时，现有方法的解码准确率会显著下降，这种情形在实际使用中十分常见。为应对这些挑战，我们提出了\textsc{XMark}，一种在LLM生成文本中编码和解码二进制信息的新方法。\textsc{XMark}的编码器独特设计能在水印token生成时产生更少失真度的logit分布，从而保持文本质量，并使其定制的解码器能够在token有限的情况下可靠恢复编码信息。在多种下游任务上的大量实验表明，\textsc{XMark}在保持水印文本质量的同时显著提升了解码准确率，超越了以往的方法。相关代码见：https://github.com/JiiahaoXU/XMark。

## Abstract
Multi-bit watermarking has emerged as a promising solution for embedding imperceptible binary messages into Large Language Model (LLM)-generated text, enabling reliable attribution and tracing of malicious usage of LLMs. Despite recent progress, existing methods still face key limitations: some become computationally infeasible for large messages, while others suffer from a poor trade-off between text quality and decoding accuracy. Moreover, the decoding accuracy of existing methods drops significantly when the number of tokens in the generated text is limited, a condition that frequently arises in practical usage. To address these challenges, we propose \textsc{XMark}, a novel method for encoding and decoding binary messages in LLM-generated texts. The unique design of \textsc{XMark}'s encoder produces a less distorted logit distribution for watermarked token generation, preserving text quality, and also enables its tailored decoder to reliably recover the encoded message with limited tokens. Extensive experiments across diverse downstream tasks show that \textsc{XMark} significantly improves decoding accuracy while preserving the quality of watermarked text, outperforming prior methods. The code is at https://github.com/JiiahaoXU/XMark.

---

## 论文详细总结（自动生成）

# XMark：用于 LLM 生成文本的可靠多比特水印 —— 论文总结

---

## 1. 研究动机与核心问题

- **背景：**  
  随着大型语言模型（LLM）在文本生成任务中的广泛使用，其输出文本的滥用风险（如虚假内容、抄袭、造假信息）显著增加。为了追踪和归属这些文本来源，研究者提出了“文本水印（watermarking）”技术。

- **核心问题：**  
  现有多比特（multi-bit）水印方法可以在 LLM 文本中嵌入二进制信息，用于来源标识与追踪，但仍存在如下瓶颈：
  - 对较长信息的编码计算成本高或不切实际。
  - 文本质量（PPL）与解码准确率（Bit Accuracy, BA）之间存在显著权衡。
  - 当可解码文本较短（token 数有限）时，准确率显著下降。
  
- **研究目标：**  
  作者提出 **XMark** 方法，以在 LLM 生成文本中实现：
  - 高效的多比特嵌入；
  - 低失真的生成质量；
  - 在短文本条件下仍保持高解码准确率。

---

## 2. 方法概述与关键技术

### 2.1 核心思想
XMark 在现有 block-wise 多比特水印（如 MPAC）基础上提出改进，通过**多重词汇表置乱与交叉绿色列表（evergreen list）**的机制，使编码扰动更小、解码更鲁棒。

### 2.2 三项核心设计
1. **Leave-One-Shard-Out (LOSO) 策略：**  
   - 将词汇表划分为 2ᵈ 个 shard，但与 MPAC 不同，XMark 选择“除去目标 shard，保留其他 shard 为绿色列表”。  
   - 绿色比例 γ = 1−2⁻ᵈ，明显高于 MPAC（γ = 2⁻ᵈ），从而减小对 logits 分布的整体扰动，提升文本自然度。

2. **Evergreen List（永绿表）：**  
   - 通过 k 个独立 hash key 生成 k 个词表置乱；  
   - 对每个置乱应用 LOSO 策略并求交集，得到 evergreen list；  
   - 该列表的期望比例约为 (1−2⁻ᵈ)ᵏ，平衡质量与鲁棒性。

3. **Constrained Token–Shard Mapping Matrix (cTMM)：**  
   - 解码时，对每个 token 建立受约束的映射矩阵，每个 token 只计入一次，从而防止过度计数；  
   - 通过统计“最少计数的 shard”作为未提升组（即编码位），提升解码可靠性，尤其在有限 token 条件下。

### 2.3 算法流程摘要（简化说明）
- **编码阶段：**
  1. 将二进制消息 m 划分为 r 块，每块 d 位；
  2. 对每步生成的 token，根据前 2 个 token 与多组 hash key 生成置乱；
  3. 构造 evergreen list，并对其中 token 的 logits 加上偏移 δ；
  4. 采样生成下一个 token；
  5. 重复至生成完整文本。

- **解码阶段：**
  1. 利用相同 hash key 重构词表分区；
  2. 对每个 token 更新 cTMM；
  3. 对每个消息块取最少计数的 shard 作为实际 bit 块；
  4. 拼接形成完整二进制消息。

---

## 3. 实验设计

### 3.1 数据与任务
在多种 LLM 下游任务中验证：
- **Text Completion（文本续写）**：C4 News (Raffel et al., 2020)
- **Story Generation（故事生成）**：WritingPrompts 数据集
- **Summarization（文本摘要）**：CNN/DailyMail
- **Machine Translation（翻译）**：WMT14 德译英

### 3.2 对比基线
与 5 个多比特水印方法全面比较：
- **MPAC**（Yoo et al., 2024）
- **RSBH**（Qu et al., 2024）
- **CycleShift**（Fernandez et al., 2023）
- **DepthW**（Li et al., 2024）
- **StealthInk**（Jiang et al., 2025）

### 3.3 模型设置
- 默认使用 LLaMA-2-7B、LLaMA-2-7B-Chat；
- 也扩展至 Qwen2.5-7B 与 LLaMA-3.1-8B；
- 评估指标：
  - 解码准确率（Bit Accuracy, BA）
  - 文本困惑度（Perplexity, PPL）
  - 语义指标：BERTScore (BSc)
  - 词面指标：ROUGE-1, ROUGE-Lsum
  - 任务指标：BLEU（翻译任务）

---

## 4. 资源与算力

- **配置说明：**
  - 实验在自建 Linux 集群上运行；
  - 使用 **8 张 NVIDIA RTX A6000 GPU（每张 48GB）**；
  - CPU 为 AMD EPYC 7763（64 核）；
  - 共计约 **两周 GPU 时长**。
- **备注：**
  - 文中未涉及额外训练，仅基于推理（inference）阶段执行水印生成与解析；
  - 无需额外模型微调。

---

## 5. 实验数量与充分性

- **任务维度：**4 个主要下游任务；
- **参数维度：**
  - 消息长度 b ∈ {8, 16, 32, 64}；
  - 生成长度 T ∈ {150, 200, 250, 300, 1000}；
  - 偏置值 δ ∈ {2, 3, 4}；
  - 哈希键数量 k ∈ {1, 2, 3, 4}；
- **扩展实验：**
  - 消融实验（不同 k, 使用 cTMM vs TMM）；
  - 鲁棒性测试（copy-paste / paraphrase 攻击）；
  - False Positive 检测；
  - 跨模型泛化（Qwen 与 LLaMA-3.1）。
- **综合性：**实验维度多且覆盖全面，计算设置一致，保证了结果的客观与可比性。

---

## 6. 主要结论与发现

- **总体性能：**
  - XMark 在所有任务中均取得最高或次高 Bit Accuracy；
  - 在 T = 150 的短文本条件下仍保持 >95% 解码准确率；
  - PPL 比 MPAC、CycleShift 等方法显著下降，文本更自然。
- **训练外推：**
  - 对长消息（b=64）仍能保持高准确率（93%）；
  - 对翻译任务中的 BLEU 影响最小（性能最高）。
- **鲁棒性：**
  - 在 copy-paste 和 paraphrase 攻击下，优于 StealthInk 与 RSBH；
  - False-positive 检测准确率高，误检率低。
- **效率：**
  - 编码时间 ≈ 11.5 s；解码仅需 0.08 s；
  - 开销与最快基线（MPAC）相当。

---

## 7. 优点与创新亮点

- **高效且低失真的设计：**
  - LOSO 策略维持高绿色比例，有效保持语言流畅度；
- **鲁棒的多哈希交叉机制：**
  - 通过 evergreen list 在有限 token 条件下保持稳定信号；
- **cTMM 解码策略创新：**
  - 高效避免 shard 过度计数，显著提升短文本解码成功率；
- **实验广度与严谨性：**
  - 涵盖多任务、多模型、多参数的系统验证；
- **轻量且通用：**
  - 无需重训练，可直接嵌入现有 LLM 输出流程。

---

## 8. 局限与不足

- **参数敏感性：**
  - 超参数 *k* 与偏置 *δ* 仍需人工调优；
- **大块长度问题：**
  - 当消息块 d 增大时，解码精度下降；
- **对抗鲁棒性仍有限：**
  - 对 paraphrase 与字符扰动攻击的抗性仍非最优；
- **未实现自适应优化：**
  - 未给出 δ 与 k 的自适应平衡算法；
- **计算假设：**
  - 主要集中在生成与解码阶段，无对训练级水印（例如调参或微调级嵌入）的探索。

---

**（完）**
