---
title: Can we Watermark Low-Entropy LLM Outputs?
title_zh: 我们能为低熵大型语言模型输出加水印吗？
authors: "Noam Mazor, Andrew Morgan, Rafael Pass"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12051v1"
tags: ["query:q8"]
score: 9.0
evidence: 大语言模型输出水印及其对抗编辑的鲁棒性
tldr: 本文探讨低熵LLM输出的水印可行性，突破传统需高熵假设的限制，在仅具常数熵率时构建了两种稳健的水印方案，可抵抗随机替换和删除攻击，扩展了LLM水印技术的适用范围。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12051v1/fig-001.webp\", \"caption\": \"Figure 1: Summary of our results compared to related results in terms of per-token entropy requirement (over a sufficiently long substring unless otherwise stated); robustness to (random or adversarial) substitutions, insertions, and deletions; and required assumptions.\", \"page\": 6, \"index\": 1, \"width\": 958, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-002.webp\", \"caption\": \"Figure 2: Protocol Gen for generating the secret information used in the watermarking protocol.\", \"page\": 19, \"index\": 2, \"width\": 948, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-003.webp\", \"caption\": \"Figure 3: Protocol Watermark for watermarking a generative model.\", \"page\": 20, \"index\": 3, \"width\": 948, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-004.webp\", \"caption\": \"Figure 4: Protocol Detect for detecting the presence of a watermark in a given message.\", \"page\": 21, \"index\": 4, \"width\": 948, \"height\": 327}]"
motivation: 研究动机是现有可证明安全的水印方法依赖高熵输出，难以应用于低熵的LLM生成内容。
method: 作者构建了针对随机替换及删除的基于LPN假设和伪随机纠错码的水印方案。
result: 提出两种在低熵条件下仍具鲁棒性的水印方案，分别应对随机替换及随机删除。
conclusion: 该工作表明即使LLM输出的熵较低，也能在合理假设下实现稳健的水印嵌入。
---

## 摘要
近年来，一个令人兴奋的研究方向致力于开发用于大型语言模型（LLMs）输出的水印方法。我们专注于可证明无法检测的水印——即不改变 LLM 输出分布的方案，同时能够在输出中嵌入一个水印，以标识该输出是由特定的 LLM 生成的。此外，这种水印应难以被删除，即使攻击者可能通过编辑、插入或删除水印输出中的 token 进行篡改。确实，近期的研究（Christ 等 [COLT'24]、Christ 等 [CRYPTO'24]、Golowich 等 [NeuroIPS'24]）展示了如何设计此类方案，使其在面对常数比例的替换甚至常数比例的任意编辑时仍具鲁棒性。然而，这些工作在 LLM 输出的熵方面做出了强假设。尤其值得注意的是，它们都要求恒定的熵率——即在输出的足够长子串中，需要有固定比例的 token 具有至少 O(log |T|) 的经验熵，其中 T 是 token 的字母表；此外，Golowich 等还要求 T 的大小大于安全参数。在本研究中，我们探讨当每个 token 的熵仅为一个常数时，是否仍能对 LLM 输出进行水印，从而去除对字母表大小或安全参数的依赖。在这一低熵情形下，我们构造了：  - 一种在随机替换下具鲁棒性的水印方案（假设次指数级 LPN，类似于 Christ 等 [CRYPTO'24]）；  - 一种在随机替换和随机删除下均具鲁棒性的水印方案，前提是额外假设 LLM 输出仅引入随机错误（与 Christ 等 [CRYPTO'24] 的假设类似），或构造一个对抗性替换与随机删除均鲁棒的伪随机纠错码。

## Abstract
A recent and exciting thread of work focuses on developing methods for watermarking the output of large language models (LLMs). We focus on provably undetectable watermarking-that is, schemes that do not alter the output distribution of the LLM, yet enable embedding a watermark in the output that identifies the output as having been generated by the particular LLM. Furthermore, the watermark should be hard to remove by an adversary that may potentially edit, insert, or delete tokens from the watermarked output. Indeed, recent work (Christ et al. [COLT'24], Christ et al. [CRYPTO'24], Golowich et al. [NeuroIPS'24]) shows how to develop such schemes that are robust against a constant fraction of substitutions, or even against a constant fraction of arbitrary edits.   These works, however, make strong assumptions on the entropy present in the output of the LLM. Most notably, they all require constant entropy rate-that is, a constant fraction of the tokens in a sufficiently long substring of the output need to have empirical entropy at least O(log |T|), where T is the alphabet of tokens, and Golowich et al. additionally require T to be larger than the security parameter. In this work, we consider whether we can also watermark the outputs of LLMs when the per-token entropy is just a constant, discarding the dependence on the alphabet size or security parameter. In this regime, we construct:   - A watermarking scheme robust against random substitutions (assuming subexponential LPN, as in Christ et al. [CRYPTO'24])   - A watermarking scheme robust against random substitutions and random deletions, given either the additional heuristic assumption that the output of the LLM only introduces random errors (analogous to the assumption made by Christ et al. [CRYPTO'24]) or a construction of a pseudorandom error-correcting code robust to adversarial substitutions and random deletions.

---

## 论文详细总结（自动生成）

# 《Can we Watermark Low-Entropy LLM Outputs?》论文总结  

---

## 1. 核心问题与研究背景  

- **研究动机**：  
  随着大型语言模型（LLM）生成的文本质量越来越接近人类写作，区分 AI 生成内容与人工内容变得困难。为防止虚假信息扩散与版权争议，研究者提出以“水印”（watermark）方式标识 AI 生成内容。  
- **核心问题**：  
  现有的“可证明安全的”水印方案假定 LLM 输出具有高熵（即 token 分布多样、接近随机），但现实中许多 LLM 输出是**低熵的**（如出现重复 token、确定性词汇）。论文因此提出关键问题：  
  > 在低熵条件下，我们是否仍能构建“不可检测且可鲁棒恢复”的水印方案？  
- **研究意义**：  
  本文是首个系统分析“低熵 LLM 输出水印可行性”的理论研究，为扩展加密型水印技术的适用范围（尤其是自然语言输出）提供了基础。

---

## 2. 方法论与核心思想  

### 2.1 总体思路  
- 作者构建了一种**基于伪随机纠错码（Pseudorandom Codes, PRC）**的水印框架。  
- 此方案在**不改变 LLM 输出概率分布**的情况下，将一个伪随机序列（codeword）隐式嵌入到输出 token 序列中。  
- 检测阶段通过相同的随机函数族（hash 函数）恢复并验证水印。  

### 2.2 技术关键点  

- **伪随机纠错码（PRC）**：  
  - PRC 由加密密钥生成随机码字。  
  - 具有“伪随机性”（对外不可区分于随机比特）与“纠错性”（在有限替换/删除下仍可恢复）。  
  - 假设基础为**次指数级线性噪声问题（subexponential LPN）**。  

- **低熵条件的关键突破**：  
  - 现有方案要求每个 token 的经验熵至少为 `Ω(log |T|)`。  
  - 本文仅需“部分 token”具备**1 bit 熵**即可嵌入水印。  
  - 将高熵要求独立于字母表大小 |T|。  

- **核心算法流程**：  
  1. **生成阶段 (Gen)**：随机生成加密密钥和一组哈希函数；  
  2. **嵌入阶段 (Watermark)**：  
     - 对 LLM 的下一 token 分布 `D`，随机取样两次（t₀, t₁）；  
     - 若两者经哈希函数映射到不同二值，则输出与 PRC 目标比特匹配的样本；  
     - 若哈希值相同，则随机输出一个，可能引入 embedding error；  
     - 均值上，约 5/8 的 token 能正确映射；  
     - 同一哈希函数用于一整块文本（block），不同块使用独立哈希以便检测。  
  3. **检测阶段 (Detect)**：  
     - 遍历所有块，哈希映射并利用 PRC 的纠错特性尝试恢复水印；  
     - 任一块成功解码则判定文本含水印。  

### 2.3 理论结果  
基于该架构，作者在不同假设下给出四个主要定理（对应表 1）：  

| 定理 | 攻击模型 | 熵要求 | 依赖假设 | 鲁棒性 |
|------|-----------|---------|-----------|---------|
| Thm.1/Cor.1 | 随机替换 | O(1) | LPN | 可抵抗(1/2−ε)随机替换 |
| Thm.2/Cor.2 | 替换 + 随机删除 | O(1) | LPN + 误差独立假设 | 可抵抗(1/2−ε)替换、(1−ε′)删除 |
| Thm.3 | 替换 + 随机删除（无假设） | O(1) | 假定存在更强 PRC | 同上 |
| Thm.4/Cor.3 | 任意编辑（插/删） | O(1) | PRC-permuted codes | 可抵抗常数比例任意编辑 |

---

## 3. 实验设计  

- **类型**：  
  本文为**理论构造与安全性证明工作**，并未使用任何真实或合成数据集进行数值实验。  
- **验证方式**：  
  所有结果均为基于复杂性假设（LPN 难题）、概率分析（Hoeffding 不等式）及编码理论的形式化证明。  
- **对比基线**：  
  - Christ & Gunn (2024, CRYPTO): 高熵水印（需 O(log|T|) 熵）。  
  - Golowich & Moitra (2024, NeurIPS): 具编辑距离鲁棒性的水印（大字母表）。  
  本文结果在相同假设下大幅放宽熵要求。  

---

## 4. 资源与算力  

- **无计算实验与模型训练**：  
  论文无 GPU、算力或训练细节说明，全部为理论分析与伪代码实现。  

---

## 5. 实验数量与充分性  

- 因研究性质为理论推导，未进行量化实验；  
- 但证明覆盖多种攻击模型（随机替换、删除、任意编辑），并通过多定理给出鲁棒性下界；  
- 从理论完备性角度而言，论证充分且体系闭合；  
- 从实践验证角度则仍缺少真实 LLM 测试。  

---

## 6. 主要结论与发现  

1. **低熵输出仍可安全加水印**：即便每个 token 平均仅 1 bit 熵，只要子串中含常数比例高熵 token 即可；  
2. **保持完全“不可检测性”**：输出分布在统计上与原模型无差异；  
3. **抗编辑鲁棒性**：  
   - 随机替换情形：抵抗超过 50% token 替换；  
   - 替换 + 删除：抵抗近 100% 删除比例（假设成立下）；  
   - 任意编辑：基于更强的 PRC 假设，可抵抗常数比例插删；  
4. **形成统一理论框架**：连接高熵（Golowich、Christ）与低熵两类方案理论。  

---

## 7. 优点与亮点  

- **突破性贡献**：首次提出低熵条件下的可证明水印方案；  
- **明确的数学分析**：使用哈希与 PRC 结合，推导出误码上界与恢复概率；  
- **单 token 级嵌入机制**：在语义层面操作，避免二进制翻译的非自然性；  
- **理论与密码学结合**：在 LPN、伪随机函数、纠错码之间建立完备推理链；  
- **鲁棒性表覆盖全面**：对比多类攻击模型。  

---

## 8. 局限与不足  

- **缺乏实证验证**：无在真实 LLM（如 GPT、LLaMA）上测试嵌入效果；  
- **依赖强假设**：部分结果依赖“输出误差可建模为二元对称信道”的启发式假设，现实中难以满足；  
- **PRC 构造依赖性强**：目前尚未存在可高效支持“对抗替换+随机删除”的实际 PRC 实例；  
- **适用性局限**：对极端低熵（接近确定性输出）的文本可能失效；  
- **可扩展性未知**：未考虑实际 tokenization 或自然语言复杂相关性引入的偏差；  
- **参数效果难量化**：理论上的常数项（δ、ε）未对应实际精度指标。  

---

**（完）**
