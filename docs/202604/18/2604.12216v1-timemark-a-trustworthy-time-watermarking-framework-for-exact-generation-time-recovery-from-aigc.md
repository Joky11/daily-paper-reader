---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一种用于从AIGC精确恢复生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 9.5
evidence: 用于AIGC生成时间恢复与真伪验证的水印技术
tldr: 本文提出了TimeMark，一个可信的时间水印框架，用于恢复AI生成内容的确切生成时间。通过引入加密技术与双阶段编码，TimeMark避免了现有方法的统计可检测性与伪造风险，实现了在司法场景下可验证、可恢复且准确的时间标识，为AIGC相关知识产权争议提供了实用解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有文本水印方法检测不确定且易被伪造，无法作为可靠司法证据。
method: 将时间信息通过受监管的加密密钥编码，并使用两阶段编码与纠错码确保恢复精度。
result: "理论与实验均显示该框架实现了100%准确的时间恢复，同时抵御用户和服务端的伪造攻击。"
conclusion: TimeMark框架实现了可验证的时间水印，可作为司法证据支持知识产权纠纷中的可靠生成时间恢复。
---

## 摘要
大型语言模型（Large Language Models，LLMs）在文本生成中的广泛应用，引发了对于知识产权纠纷的日益担忧。水印技术可以将元信息嵌入到人工智能生成内容（AIGC）中，具有作为司法证据的潜力。然而，现有方法依赖于词元分布中的统计信号，导致检测结果本质上是概率性的，在多比特编码（例如时间戳）情况下可靠性较低。此外，这些方法会引入可检测的统计模式，使其易受伪造攻击，并使模型提供方能够伪造任意水印。为了解决这些问题，我们提出了“可信水印”（trustworthy watermark）的概念，该方法实现了100%识别准确率的可靠恢复，同时能够抵抗用户侧的统计攻击和提供方的伪造。我们重点研究用于司法证据的可信时间水印（trustworthy time watermarking）。所提出的框架结合了密码学技术，并在监管监督下将时间信息编码到与时间相关的秘密密钥中，从而防止任意时间戳伪造。水印负载与时间解耦，并为每个实例生成随机的、非存储的比特序列，从而消除统计模式。为了确保可验证性，我们设计了一个双阶段编码机制，并结合纠错码，使生成时间的恢复在理论上达到完美准确。理论分析和实验结果均表明，我们的框架满足司法证据的可靠性要求，并为未来AIGC相关的知识产权纠纷提供了一种切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# TimeMark：用于从AIGC精确恢复生成时间的可信时间水印框架  
（Shangkun Che, Silin Du, Ge Gao，2026）

---

## 一、核心问题与研究背景

- **研究动机**：  
  随着大型语言模型（LLMs）广泛用于文本创作、报告撰写等高价值场景，知识产权（IP）纠纷愈加频发。传统的数字证据体系难以对 AI 生成内容（AIGC）的“生成时间”进行可信证明。  
  - AIGC 通常无固定文件载体，也缺乏内部时间戳记录。
  - 现有 LLM 水印技术多用于“是否为 AI 生成”的零比特检测，无法**精确恢复生成时间**。  
  - 关键挑战包括：
    1. 多比特水印恢复准确度不足，无法满足司法标准；
    2. 水印含统计模式，易被生成端或用户伪造；
    3. 模型服务商自身可篡改时间戳并伪造证据。

- **总体目标**：  
  建立一个能在司法场景下作为**可靠证据**使用的时间水印框架，使得 AIGC 的生成时间可以**100%准确恢复且不可伪造**。

---

## 二、方法论：核心思想与关键技术

### 1. 基本思想

TimeMark 框架提出“**可信时间水印（Trustworthy Time Watermark）**”概念，其设计目标是：
- 完美恢复生成时间；
- 防止用户侧统计伪造与服务端伪造；
- 满足司法证据的真实性、可靠性、可验证性等要求。

### 2. 技术关键点

- **时间依赖密钥机制**：  
  每个时间窗口（如按分钟计）对应唯一密钥 `K_t`，通过**单向哈希链**生成：  
  `K_t = H(K_{t-1})`  
  Hash 函数不可逆，保证服务方无法生成过去的密钥，从而不能伪造过去时间戳。

- **受监管的硬件安全模块（HSM）管理密钥**：  
  记录时间序列密钥，提供受审计的安全存储与访问；确保时间密钥仅能在验证阶段使用。

- **随机水印负载与时间解耦**：  
  每次生成随机比特序列 `R ∈ {0,1}^m`，再经纠错编码扩展为 `P = ECC(R)`。  
  时间信息不直接入负载，而与密钥 K_t 绑定，实现抗统计攻击。

- **双阶段水印嵌入机制**：  
  - *阶段一（前α|P| token）*：  
    生成种子 `S1_i = PRF(K_t, R, h(prefix_i))`；根据种子随机划分词表，设定绿/红列表，并按偏置 δ 修改 logits。  
  - *阶段二（后L−α|P| token）*：  
    种子不再使用 R，只依赖 `S2_i = PRF(K_t, h(prefix_i))`，帮助验证阶段重建。

- **解码与验证过程**：
  1. **Step 1：** 根据第二阶段重建分区，使用多数投票恢复负载 P；由 ECC 解出随机串 `ˆR`。
  2. **Step 2：** 用 `ˆR` 与候选密钥重建种子，计算匹配率 `Score(K_t, ˆR)`；阈值 `ϕ=0.65` 决定是否为正确时间。

- **纠错码（如 BCH(63,10)）作用**：  
  容忍多达 13 个比特误差，确保随机负载可精确恢复。

---

## 三、实验设计

- **实验场景**：  
  聚焦真实的 AIGC 文本生成任务（创意与报告类）。共使用 20 个 prompt 任务。每次生成：
  - 一份带水印文本；
  - 一份无水印文本。  

- **模型与参数**：  
  使用 **Qwen2.5-7B** 语言模型。  
  随机负载长度 10 位，经 BCH 扩展为 63 位；  
  文本长度 ≥945 tokens；α=5；偏置 δ=2.5；验证阈值 ϕ=0.65。

- **候选密钥集合**：  
  包含真实时间及相邻 4 个时间窗口，共 5 个候选进行验证。

- **对比基线**：  
  文中未直接引用外部 benchmark，而通过无水印文本与错误密钥进行自校验对比，验证准确率与伪造检测能力。

---

## 四、资源与算力

- 文中仅说明使用了 **Qwen2.5-7B 模型** 进行生成实验，未披露 GPU 型号、数量、训练时长等算力信息。  
  → **推断**：方法为推理阶段嵌入，不涉及模型再训练，因此算力需求较低，属于部署级实验。

---

## 五、实验数量与充分性

- **实验总量**：  
  800 次生成试验 × 2（有水印/无水印），共 1600 文本样本。  
  覆盖 20 prompt 任务。每次进行 5 候选时间验证。

- **实验充分性与公平性**：  
  - 实验量较大，验证涵盖多种文本任务；
  - 未涉及跨模型、语义变化或编辑鲁棒性测试；
  - 实验目的为验证准确率与区分性，主观偏差较小，但场景覆盖仍有限。

---

## 六、主要结论与发现

- **核心结果**：
  - 100% 准确恢复生成时间（800/800 正确识别）；
  - 零误检率（非水印文本无任何时间识别结果）；
  - 平均验证得分显著区分：  
    - 水印文本：0.77  
    - 无水印文本：0.50。

- **理论证明**：  
  BCH 编码在参数 δ=2.5 下，错误恢复概率 <10⁻³⁴，验证错误概率 <10⁻⁴⁴（正确密钥），错误接受概率 <10⁻⁸（错误密钥）。

→ 结论：TimeMark 可在司法场景下提供**理论上完美可靠**的生成时间证明。

---

## 七、优点与创新亮点

- **技术创新**
  - 时间密钥与负载随机化结合，彻底切断时间与文本统计关联；
  - 双阶段编码结构提高可验证性；
  - 结合 HSM 与单向密钥链，防止服务商篡改；
  - 纠错码增强容错与恢复精度。

- **司法可用性**
  - 满足法律条款对应的证据要求（真实性、完整性、可核查性、非事后制造）。
  - 可作为**知识产权争议中的司法证据**。

- **防攻击性强**
  - 抗统计学习与仿造攻击（理论与实验验证药效显著）；
  - 抗服务端恶意伪造（时间链不可回溯）。

---

## 八、不足与局限

- **文本长度要求高**：  
  目前仅支持长文本（≥945 tokens），短文本场景尚未验证。

- **缺乏编辑鲁棒性**：  
  假设用户不对生成文本进行修改，缺少 paraphrasing 或摘要等变形测试。

- **未涉及多模型对比与开放域应用**：  
  实验主要基于单一模型 Qwen2.5-7B，未验证跨模型兼容性。

- **算力与运行性能未详述**：  
  虽为推理级方法，但缺少对效率与实时性的评估。

- **司法实践仍需制度配套**：  
  如外部监管机构对 HSM 的认证与审计需进一步落地。

---

（完）
