---
title: "UniMark: Unified Adaptive Multi-bit Watermarking for Autoregressive Image Generators"
title_zh: UniMark：用于自回归图像生成器的统一自适应多比特水印方案
authors: "Yigit Yilmaz, Elena Petrova, Mehmet Kaya, Lucia Rossi, Amir Rahman"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.11843v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于自回归图像生成器的统一水印技术，以保护所有权
tldr: 本文提出UniMark，一个针对自回归图像生成的统一自适应多比特水印框架，通过语义分组、块级编码和统一接口三项创新，解决现有方法在信息容量、安全性与通用性上的不足，实验表明该方法在图像质量、水印检测与鲁棒性方面均实现了领先表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11843v1/fig-001.webp\", \"caption\": \"Figure 4: Security analysis: (a) green ratio distributions under correct/wrong keys; (b) forgery success rate vs. partition exposure fraction.\", \"page\": 11, \"index\": 1, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-002.webp\", \"caption\": \"Figure 5: Efficiency analysis: (a) absolute time comparison (log scale); (b) relative overhead percentage.\", \"page\": 11, \"index\": 2, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-003.webp\", \"caption\": \"Figure 2: Radar chart comparing robustness (TPR@FPR=1%) across six attack types on LlamaGen.\", \"page\": 10, \"index\": 3, \"width\": 392, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-004.webp\", \"caption\": \"Figure 3: Parameter sensitivity analysis: effect of green ratio γ on (a) FID, (b) TPR@FPR=1%, and (c) 32-bit extraction accuracy.\", \"page\": 10, \"index\": 4, \"width\": 822, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-005.webp\", \"caption\": \"Table 1: Image quality comparison across AR models. ∆FID denotes the FID increase compared to unwatermarked generation.\", \"page\": 8, \"index\": 5, \"width\": 636, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-006.webp\", \"caption\": \"Figure 6: Attack strength analysis: (a) JPEG quality, (b) Gaussian noise σ, (c) crop ratio vs. detection and extraction performance.\", \"page\": 12, \"index\": 6, \"width\": 821, \"height\": 220}]"
motivation: 现有自回归图像水印方法仅能进行二值验证、易受攻击且缺乏跨架构通用性。
method: 提出UniMark框架，结合自适应语义分组、块级多比特编码及统一的令牌替换接口，实现多模型支持的水印嵌入。
result: UniMark在三种自回归模型上达到了最佳的图像质量和检测精度，并在多种扰动攻击下保持稳健。
conclusion: UniMark为自回归图像生成的隐形多比特水印提供了统一、稳健的解决方案，显著提升了可扩展性和安全性。
---

## 摘要
针对自回归（AR）图像生成的隐形水印技术近年来受到广泛关注，它被视为一种保护图像所有权、追踪人工智能生成内容的手段。然而，现有方法存在三个主要局限性：（1）仅能嵌入用于二元验证的零比特水印，无法传递多比特信息；（2）依赖静态的代码簿划分策略，一旦划分方式被暴露便容易遭受安全攻击；（3）针对特定的AR架构设计，缺乏在不同AR范式间的泛化能力。我们提出了\method{}，一种无需训练的统一自回归图像生成水印框架，用于解决上述三大问题。\method{}包含三个核心组件：\textbf{自适应语义分组（ASG）}，它根据语义相似性和密钥动态划分代码簿条目，以同时保证图像质量和安全性；\textbf{分块多比特编码（BME）}，通过将token序列划分为块并在不同块中利用纠错码编码比特，实现可靠的信息传输；以及\textbf{统一token替换接口（UTRI）}，该接口抽象化水印嵌入过程，兼容下一token预测（如LlamaGen）和下一scale预测（如VAR）范式。我们提供了关于检测错误率和嵌入容量的理论分析。基于三个AR模型的大量实验表明，\method{}在图像质量（FID）、水印检测准确率和多比特信息提取方面均达到最新的先进水平，同时在裁剪、JPEG压缩、高斯噪声、模糊、颜色抖动以及随机擦除等攻击下保持良好鲁棒性。

## Abstract
Invisible watermarking for autoregressive (AR) image generation has recently gained attention as a means of protecting image ownership and tracing AI-generated content. However, existing approaches suffer from three key limitations: (1) they embed only zero-bit watermarks for binary verification, lacking the ability to convey multi-bit messages; (2) they rely on static codebook partitioning strategies that are vulnerable to security attacks once the partition is exposed; and (3) they are designed for specific AR architectures, failing to generalize across diverse AR paradigms. We propose \method{}, a training-free, unified watermarking framework for autoregressive image generators that addresses all three limitations. \method{} introduces three core components: \textbf{Adaptive Semantic Grouping (ASG)}, which dynamically partitions codebook entries based on semantic similarity and a secret key, ensuring both image quality preservation and security; \textbf{Block-wise Multi-bit Encoding (BME)}, which divides the token sequence into blocks and encodes different bits across blocks with error-correcting codes for reliable message transmission; and \textbf{a Unified Token-Replacement Interface (UTRI)} that abstracts the watermark embedding process to support both next-token prediction (e.g., LlamaGen) and next-scale prediction (e.g., VAR) paradigms. We provide theoretical analysis on detection error rates and embedding capacity. Extensive experiments on three AR models demonstrate that \method{} achieves state-of-the-art performance in image quality (FID), watermark detection accuracy, and multi-bit message extraction, while maintaining robustness against cropping, JPEG compression, Gaussian noise, blur, color jitter, and random erasing attacks.

---

## 论文详细总结（自动生成）

# UniMark：用于自回归图像生成器的统一自适应多比特水印框架 — 中文结构化总结

---

## 1. 核心问题与研究背景

- **研究动机**：随着自回归（Autoregressive, AR）图像生成模型（如 LlamaGen、VAR、MAGVIT 等）生成能力接近甚至超越扩散模型，AI 生成图像的来源追踪与版权保护成为紧迫问题。  
- **现有问题**：
  1. **零比特局限**：目前多数方法仅支持判断“是否带水印”，不能携带用户标识等多比特信息。
  2. **静态划分风险**：如 IndexMark 的静态红绿分组一旦被攻击者获知，即失去安全性。
  3. **架构依赖性强**：现有方案常针对特定 AR 范式设计，缺乏跨模型通用性。
- **研究目标**：提出一种**无需训练、跨模型、可嵌入多比特信息且具备鲁棒性**的统一水印框架，用于自回归图像生成任务。

---

## 2. 方法论：UniMark 框架

UniMark 由三大核心模块构成，分别解决安全性、信息承载能力及架构通用性问题。

### 2.1 自适应语义分组（Adaptive Semantic Grouping, ASG）

- **核心思想**：  
  根据 *语义相似性* 和 *密钥依赖性* 动态划分代码簿（codebook），为每个 token 位置生成独特的红（R）/绿（G）组。
- **关键机制**：
  - 使用代码簿嵌入向量计算余弦相似矩阵；
  - 对每个位置 i，利用哈希 `seed_i = H(κ ∥ i)` 生成伪随机置换；
  - 前 γK 个条目形成绿色集合 Gi，防止静态攻击；
  - 若 token ti ∈ Ri，则替换为 Gi 中语义最相近的元素以保持视觉一致性。
- **效果**：在保证视觉质量的同时，通过密钥控制分组实现安全性与多样性。

### 2.2 分块多比特编码（Block-wise Multi-bit Encoding, BME）

- **目标**：将消息比特可靠地嵌入 token 序列。
- **步骤说明**：
  1. 对消息 m 进行 BCH 编码 → m′；
  2. 将 token 序列划分为 n 个块 Bj；
  3. 若当前块对应比特为 1 → 使用绿色集 Gi；若为 0 → 使用红色集 Ri；
  4. 检测时统计绿色比例 rj，并通过阈值 τ=0.5 判断比特；
  5. 最终通过 BCH 解码纠正误差。
- **理论分析**：论文提出嵌入容量与误检率公式，证明在 γ=0.5 条件下容量与 token 数 N 成线性关系。

### 2.3 统一 Token 替换接口（Unified Token-Replacement Interface, UTRI）

- **作用**：解耦具体生成模式与水印逻辑，实现跨架构支持。
- **定义三个通用操作**：
  1. `GET TOKEN SEQUENCE()`：获取原始 token 序列；
  2. `REPLACE TOKENS()`：在代码簿分组上实施替换；
  3. `DECODE IMAGE()`：解码为带水印图像。
- **适配场景**：
  - *Next-token prediction*（如 LlamaGen）；
  - *Next-scale prediction*（如 VAR），可跨不同尺度拼接处理。

### 2.4 理论分析结果

- **误报率（False Positive Rate）**基于中心极限定理推导为显著水平 α；
- **容量上限（Embedding Capacity）**与 token 数量、BCH 编码率、检测置信度等参数相关。

---

## 3. 实验设计

### 3.1 模型与数据

- 实验覆盖三种代表性自回归模型：
  1. **LlamaGen-XL**（Next-token 类型，K=16384, N=256）
  2. **VAR-d20**（Next-scale 类型，K=4096, N=680）
  3. **Open-MAGVIT2**（Next-token 类型，K=262144, N=256）
- 生成数据集：**ImageNet 验证集**样本；
- 总样本量：每模型生成 **5,000 张**图像。

### 3.2 对比基线

| 方法 | 特点 |
|------|------|
| IndexMark (2025) | 静态红绿分组，零比特验证 |
| AR-Watermark (2025) | LLM 水印思想扩展至图像 |
| Tree-Ring (2023) | 扩散模型水印，用于横向参照 |

### 3.3 评估指标

- 图像质量：FID↓、PSNR↑、SSIM↑  
- 水印性能：TPR@FPR=1%↑、Bit Accuracy↑  
- 鲁棒性测试：JPEG 压缩、高斯噪声、模糊、裁剪、颜色抖动、随机擦除六类攻击。

---

## 4. 资源与算力说明

- **算力使用**：论文未明确说明具体 GPU 型号、显存或运行时间；
- 文中仅提到“每张图像嵌入水印的平均时延约 8–15 毫秒”，相当于 **1.2–1.9% 的额外开销**，可视为推理级别开销，无需额外训练。

---

## 5. 实验数量与充分性

- **实验覆盖**：
  - 三种自回归架构 × 多种攻击类型 × 多比特长度测试；
  - 参数敏感性分析（γ 取 0.3–0.7）；
  - 消融实验（去除 ASG / BME / 纠错模块 / UTRI）；
  - 安全性、效率及容量分析。
- **充分性与公平性**：
  - 对比方法使用相同模型与数据集；
  - 结果报告包含均值与标准化指标；
  - 内容涵盖定量性能、消融与安全分析，实验设计较为全面客观。

---

## 6. 主要结论与发现

1. **图像质量保持优异**：FID 增加幅度 ≤0.09，视觉上难以区别；
2. **检测性能达 SOTA**：TPR@FPR=1% 超 99.7%，稳定跨模型；
3. **多比特嵌入稳定**：32-bit 嵌入位准确率≈99%，64-bit 保持>95%；
4. **鲁棒性强**：在六类攻击下均显著优于 IndexMark 和 AR-Watermark；
5. **安全性增强**：密钥依赖的分组显著降低伪造成功率（≤2%）；
6. **效率高**：嵌入过程轻量化，可嵌入于推理流程中。

---

## 7. 方法及实验亮点

- **多维创新**：
  - 第一次实现**训练自由、架构无关**的自回归模型多比特水印；
  - ASG 机制结合语义相似性和密钥哈希，兼顾质量与安全；
  - BME 利用 BCH 编码提升抗攻击冗余；
  - UTRI 实现算法层的通用性。
- **系统性评估**：从质量、鲁棒性、安全性到效率均有定量分析；
- **可理论支撑**：通过定理给出误报率与容量上限的闭式表达。

---

## 8. 不足与局限

- **容量限制**：嵌入容量受限于 token 长度与代码簿规模；
- **未覆盖视频与多模态任务**：仅验证单帧图像生成；
- **未验证对抗攻击场景**：如针对性解码器或再生成攻击；
- **算力与实时场景描述略有限**：缺少对大规模生成的实际部署细节；
- **参数依赖性**：尽管γ=0.5效果最佳，但在极端条件下仍需调参。

---

**（完）**
