---
title: ADD for Multi-Bit Image Watermarking
title_zh: 用于多比特图像水印的 ADD 方法
authors: "An Luo, Jie Ding"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11491v1"
tags: ["query:q6"]
score: 10.0
evidence: 多比特图像水印技术，用于识别生成图像的来源并验证其真实性。
tldr: 本文针对虚假生成图像的真实性问题提出ADD多比特图像水印方法，通过学习的水印与信息线性结合并内积解码，实现高容量、高鲁棒性及高效率水印嵌入与识别，在MS-COCO上显著超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11491v1/fig-001.webp\", \"caption\": \"Table 2: Watermark detection results. All methods are evaluated on the same 10,000 randomly sampled image–message pairs from the MS-COCO test split. PSNR (dB, higher is better) is reported as mean. AUROC (%, higher is better) is reported under each distortion setting. The Average column reports the mean AUROC across all distortion settings. DwtDct/HiDDeN/SSL use dictionary-based detection with dictionary D (dmin = 6) consisting of all 10,000 embedded 48-bit messages (threshold γ ∈ {0, . . . , 48} on best-match score). ADD (w/o D) uses the dictionary-agnostic statistic S (threshold sweep on S). ADD (w/ D) uses the dictionary-dependent statistic SD (threshold sweep on SD).\", \"page\": 25, \"index\": 1, \"width\": 964, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-002.webp\", \"caption\": \"Table 3: Runtime comparison of watermark embedding and decoding. We report average time per image (ms/image) ± standard error for the watermark embedding step (embedding a multi-bit watermark m into an image) and the decoding step (recovering the embedded bits m̂ from the watermarked image). All methods were evaluated on the same 1,000 images from the MS-COCO test split using a single NVIDIA A100 GPU. Standard errors are computed across processing batches (batch size 64), treating each batch’s per-image time as one observation.\", \"page\": 26, \"index\": 2, \"width\": 764, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-003.webp\", \"caption\": \"Figure 1: An overview of multi-bit image watermarking. An image, which may be generated by AI models or created as digital artwork such as paintings or photographs, can be embedded with a multi-bit message. Such messages may encode information such as a timestamp, IP address, signature, or private key. The resulting watermarked image is then distributed over the Internet and may undergo distortions (e.g., compression or rotation). Given a possibly distorted image, the goal is to detect whether the image is watermarked and further decode the embedded multi-bit message to recover the information.\", \"page\": 3, \"index\": 3, \"width\": 946, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-004.webp\", \"caption\": \"Figure 4: Empirical trade-offs from β and n. Trade-off between PSNR and average bit accuracy from (a) different β values and (b) different training sample sizes n. In (a), n = 2000 and β ∈ {1, 10, 100, 500, 1000, 5000, 10000}. In (b), β = 1000 and n ∈ {10, 15, 20, 50, 100, 500, 1000, 2000}. The dashed red lines indicate acceptable thresholds chosen to reflect practical deployment requirements: PSNR = 32 dB in (a) and average bit accuracy = 0.99 in (b). The rest of the training setup is the same.\", \"page\": 28, \"index\": 4, \"width\": 979, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of ADD for multi-bit image watermarking. Given an image x ∈ X , a K-bit message m ∈ {±1}K is embedded by x̃ = x+∑K k=1 mkwk. After distribution, the watermarked image may be distorted by an distortion operator A, yielding the observed image x̃′ i = A(x̃). Detection is performed to decide whether x̃′ is watermarked by ADD and if detected, decoding is performed to recover m. Specifically, detection and decoding are based on the inner products Γk = ⟨wk, x̃ ′⟩, collected as I = (Γ1, . . . ,ΓK)⊤: detect with S = ∑K k=1 |Γk|, and decode with m̂k = sign (Γk). As indicated in the dashed region, when an optional dictionary of embedded messages D ⊂ {±1}K is available, detection and decoding can be improved with SD = maxm∈D⟨m, I⟩ and m̂ = arg maxm∈D⟨m, I⟩. The watermark w1:K is trained from the top objective Ln as proposed in (5).\", \"page\": 8, \"index\": 5, \"width\": 977, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-006.webp\", \"caption\": \"Table 4: Cross-dataset generalization. Our watermark is trained on MS-COCO only and evaluated on multiple test domains, each with 1000 images. We report PSNR (mean ± standard error), average bit accuracy, and average AUROC. Bit accuracy and AUROC are averaged over the performance under each distortion setting. See detailed per-distortion result in Section I of the supplementary material.\", \"page\": 27, \"index\": 6, \"width\": 902, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-007.webp\", \"caption\": \"Table 1: Watermark decoding results. All methods are evaluated on the same 1000 randomly sampled image–message pairs from the MS-COCO test split. PSNR (dB, higher is better) is reported as mean ± standard error. Bit accuracy (%, higher is better) is reported in under each distortion setting. The Average column reports the mean bit accuracy across all distortion settings.\", \"page\": 24, \"index\": 7, \"width\": 964, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2604.11491v1/fig-008.webp\", \"caption\": \"Figure 3: Qualitative comparison of image quality across watermarking methods. All methods are evaluated on the same image, with PSNR values reported. Our method is visually close to the original image, as the rest of the methods do. A visual comparison of the magnified pixel-wise differences is in Section G of the supplementary material.\", \"page\": 24, \"index\": 8, \"width\": 976, \"height\": 231}]"
motivation: 生成模型使虚假高保真图像泛滥，亟需可靠多比特水印技术保障真实性与来源追溯。
method: "提出ADD（Add, Dot, Decode）双阶段方法，将学习到的水印线性嵌入并通过内积解码。"
result: "在MS-COCO上48比特水印任务中实现100%解码准确率，且在多种失真下精度下降不超过2%。"
conclusion: ADD方法兼具理论可解释性与计算优势，为多比特图像水印提供高效可靠的新方案。
---

## 摘要
随着生成模型能够快速生成高保真图像，社会对于虚假信息与真实性的担忧日益加剧。一个有前景的解决方案是多比特图像水印技术，它将多比特信息嵌入图像中，使验证者能够检测图像是否由某人生成，并通过解码嵌入的信息进一步识别来源。现有方法通常在容量、对常见图像失真鲁棒性以及理论依据方面存在不足。为解决这些局限，我们提出 ADD（Add, Dot, Decode），一种具有两阶段的多比特图像水印方法：首先学习一个水印并将其与多比特信息线性组合后添加到图像中；然后通过带水印图像与学习到的水印之间的内积进行解码。在标准 MS-COCO 基准上，我们展示了在具有挑战性的 48-bit 水印任务中，ADD 实现了 100\% 的解码准确率，在多种图像失真条件下性能最多下降 2\%，显著低于当前最先进方法平均 14\% 的下降。此外，ADD 在计算效率上也具有显著优势，嵌入速度提高 2 倍，解码速度提高 7.4 倍。我们进一步提供了理论分析，解释为何学习到的水印及其对应的解码规则是有效的。

## Abstract
As generative models enable rapid creation of high-fidelity images, societal concerns about misinformation and authenticity have intensified. A promising remedy is multi-bit image watermarking, which embeds a multi-bit message into an image so that a verifier can later detect whether the image is generated by someone and further identify the source by decoding the embedded message. Existing approaches often fall short in capacity, resilience to common image distortions, and theoretical justification. To address these limitations, we propose ADD (Add, Dot, Decode), a multi-bit image watermarking method with two stages: learning a watermark to be linearly combined with the multi-bit message and added to the image, and decoding through inner products between the watermarked image and the learned watermark. On the standard MS-COCO benchmark, we demonstrate that for the challenging task of 48-bit watermarking, ADD achieves 100\% decoding accuracy, with performance dropping by at most 2\% under a wide range of image distortions, substantially smaller than the 14\% average drop of state-of-the-art methods. In addition, ADD achieves substantial computational gains, with 2-fold faster embedding and 7.4-fold faster decoding than the fastest existing method. We further provide a theoretical analysis explaining why the learned watermark and the corresponding decoding rule are effective.

---

## 论文详细总结（自动生成）

# 《用于多比特图像水印的 ADD 方法》详细总结

---

## 一、研究问题与背景

- **核心问题**：随着生成模型（如扩散模型、生成对抗网络等）能快速产生高保真图像，社会对虚假内容、版权侵犯、数据来源追踪的需求急剧上升。  
- **研究动机**：如何在视觉不损伤的前提下，将多比特信息可靠地嵌入图像，并在经历压缩、旋转、噪声等失真后还能正确检测与解码，是当前图像水印研究的核心难题。  
- **现有缺陷**：
  - 多比特水印容量有限（通常 ≤ 32 bits）；
  - 对常见图像失真的鲁棒性不足；
  - 多数深度学习方法缺乏理论解释；
  - 特定模型（如扩散模型）专用的水印方案难以泛化。  

因此，论文提出一种**通用、高容量、具理论支撑的水印框架**——**ADD（Add, Dot, Decode）**，以解决容量、鲁棒性与理论可解释性之间的平衡。

---

## 二、方法论：ADD（Add, Dot, Decode）

### 1. 核心思想
- 将**K 比特信息**视为 ±1 的二值向量 \( m \in \{-1, 1\}^K \)。  
- 学习一个 **水印向量组** \( w_1, w_2, ..., w_K \in \mathbb{R}^D\)，用于与图像向量线性组合。  
- **嵌入阶段（Add）**：  
  \[
  \tilde{x} = x + \sum_{k=1}^{K} m_k w_k
  \]
  将加权水印直接加入图像像素空间。
- **解码阶段（Dot）**：  
  利用内积 \(\Gamma_k = \langle w_k, \tilde{x}' \rangle\) 提取嵌入信号，即便图像经过失真 \(A(\cdot)\)。  
- **判定阶段（Decode）**：
  - 若无消息词典：基于检测统计量 \(S = \sum_k |\Gamma_k|\) 进行显著性检验；
  - 若有词典 \(D \subseteq \{\pm1\}^K\)：基于  
    \[
    S_D = \max_{m\in D} \langle m, \Gamma \rangle
    \]
    决定水印存在与否并输出最可能的消息：
    \[
    \hat{m} = \arg\max_{m\in D} \langle m, \Gamma \rangle
    \]

### 2. 水印学习（训练阶段）
- 优化目标平衡三方面：
  - **质量（Quality）**：约束水印幅度，通过正则项 \(\beta \sum \|w_k\|^2\) 保持视觉保真；
  - **可识别性（Identifiability）**：使用**边界损失函数**（如 hinge 或 logistic loss）确保每比特可正确分类；
  - **鲁棒性（Resilience）**：通过模拟失真 \(A\) 的期望，强化在多种图像退化下仍能恢复信息。  
- 总体期望目标函数：
  \[
  L(w) = E_{x,A,m}\Big[\sum_k V(m_k \langle w_k, A(x+\sum_j m_j w_j)\rangle)\Big] + \beta\sum_k \|w_k\|^2
  \]
  
### 3. 理论基础
- 在假设图像分布近似于低维高斯子空间 \(X = BZ + \epsilon\) 下，ADD 学到的水印满足：
  - \(w_k\) 与图像主子空间正交；
  - 不同比特的水印互相正交；
  - 由此推导出最优检测统计量与极大似然解码规则。  
- 理论部分还证明：有限样本学习的水印在样本数量 \(n \to \infty\) 时，其检测率、误检率和比特准确率收敛到对应的理论最优性能。

---

## 三、实验设计

### 1. 数据集与场景
- **主训练与评估集**：MS-COCO 数据集（118,000 张训练图像，256×256 RGB）。  
- **跨域测试**：在 ImageNet、CIFAR-10、CIFAR-100 上验证泛化性能。  

### 2. 任务设定
- 多比特嵌入任务，容量 \(K = 48\)。  
- 涉及多种失真类型：高斯模糊、JPEG压缩、亮度/对比度调整、高斯噪声、旋转、裁剪、遮挡等。  

### 3. 基线方法
- **DwtDct (Al-Haj 2007)**：传统频域水印。  
- **HiDDeN (Zhu et al. 2018)**：经典深度学习水印网络。  
- **SSL (Fernandez et al. 2022)**：基于自监督特征空间优化的水印法。  

### 4. 评测指标
- 图像质量：**PSNR**；
- 检测性能：**AUROC**；
- 解码性能：**Bit Accuracy**；
- 同时评估在失真下的鲁棒性与运算效率。

---

## 四、资源与算力

- 明确说明：
  - 所有实验在 **单块 NVIDIA A100 GPU** 上执行；
  - 运行时间（Table 3）：
    - **嵌入阶段**：约 0.76 ms/图像；
    - **解码阶段**：约 0.19 ms/图像；
  - 批量大小 64。  
- 文中未披露训练总时长与 GPU 核心数的更多细节。

---

## 五、实验数量与充分性

- **主实验**：
  - 在 MS-COCO 上进行了完整对比表（性能及可视化各 9 种失真）；  
  - 还包含检测（表2）、运行时（表3）、跨数据集迁移（表4）等多维实验。  
- **消融与分析**：
  - 讨论超参数 β 与样本量 n 的影响；
  - 验证理论部分提出的正交性质及其对性能的贡献。  
- **充分性评价**：
  - 实验覆盖面广，验证了在多环境、多数据、多指标下的鲁棒表现；
  - 但未提供复杂攻击（如对抗扰动或多重压缩）的评估。

---

## 六、主要结论与发现

- 在 **48-bit 水印任务**上达到 **100% 解码准确率**，在所有失真下性能下降不超过 **2%**；
- 相比 HiDDeN, SSL 等方法，平均性能优势约 **>15%**；
- 嵌入与解码速度分别提升 **2 倍、7.4 倍**；
- 学习到的水印具有正交几何结构并与图像主成分子空间分离；
- 理论结果证明有限样本情况下仍能收敛至最优检测性能；
- 简单加法与内积机制即可实现高鲁棒、低复杂度的多比特水印系统。

---

## 七、方法与实验亮点

- **简洁的结构设计**：无需复杂神经网络，仅依赖线性加法与内积操作；
- **统一理论框架**：从几何与统计角度解释鲁棒性来源；
- **高效实现**：运算量低、速度快，可直接嵌入于生成服务端；
- **跨域泛化性强**：单次训练水印在不同数据集上仍保持>99%准确率；
- **参数可解释**：正则化参数 β 控制质量与稳健性间的权衡。

---

## 八、不足与局限

- **实验限制**：
  - 主要基于 256×256 彩色图像，尚未验证在超高分辨率或视频场景；
  - 对复杂攻击（如几何变化+噪声组合）未详细分析；
  - 实验 GPU 单一，未说明大规模部署可扩展性；
- **理论假设理想化**：
  - 低维高斯子空间模型简化了真实图像分布；
  - 未探讨在非线性流形假设下模型准确性；
- **应用限制**：
  - 假设同一水印机制被唯一机构控制，尚未处理多机构共存、冲突检测等情景。

---

**（完）**
