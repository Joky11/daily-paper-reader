---
title: Can we Watermark Low-Entropy LLM Outputs?
title_zh: 我们能否为低熵的大型语言模型输出加水印？
authors: "Noam Mazor, Andrew Morgan, Rafael Pass"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12051v1"
tags: ["query:q8"]
score: 9.0
evidence: 大语言模型输出水印及其对抗编辑的鲁棒性
tldr: 本文研究在大型语言模型输出熵较低的情况下是否能实现不可检测水印，提出了两种水印方案，分别针对随机替换和随机替换加删除场景，在相关复杂性假设下保持鲁棒性，为低熵LLM输出的水印提供了可行方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12051v1/fig-001.webp\", \"caption\": \"Figure 1: Summary of our results compared to related results in terms of per-token entropy requirement (over a sufficiently long substring unless otherwise stated); robustness to (random or adversarial) substitutions, insertions, and deletions; and required assumptions.\", \"page\": 6, \"index\": 1, \"width\": 958, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-002.webp\", \"caption\": \"Figure 2: Protocol Gen for generating the secret information used in the watermarking protocol.\", \"page\": 19, \"index\": 2, \"width\": 948, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-003.webp\", \"caption\": \"Figure 3: Protocol Watermark for watermarking a generative model.\", \"page\": 20, \"index\": 3, \"width\": 948, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-004.webp\", \"caption\": \"Figure 4: Protocol Detect for detecting the presence of a watermark in a given message.\", \"page\": 21, \"index\": 4, \"width\": 948, \"height\": 327}]"
motivation: 研究动机是探讨在语言模型输出熵较低时是否仍能实现稳健的不可检测水印。
method: 论文构建了两种低熵条件下的不可检测水印方案，分别针对随机替换和随机替换加删除。
result: 研究提出的方案在假设条件下可对随机替换或替换加删除保持鲁棒性。
conclusion: 结果表明低熵输出也可以通过合理设计的加密和纠错技术实现鲁棒的不可检测水印。
---

## 摘要
近期一项令人兴奋的研究方向专注于开发用于对大型语言模型（LLMs）输出进行水印标记的方法。我们关注的是可被严格证明为不可检测的水印方案——即不改变 LLM 的输出分布，却能在输出中嵌入一种能识别该输出来自特定 LLM 的水印。此外，该水印应难以被试图通过编辑、插入或删除标记来修改水印输出的对手去除。事实上，近期的研究（Christ 等人 [COLT'24]，Christ 等人 [CRYPTO'24]，Golowich 等人 [NeurIPS'24]）已展示了如何构建这样的方案，使其对恒定比例的替换，甚至是任意编辑，具有鲁棒性。然而，这些工作都对 LLM 输出的熵水平做出强假设。最显著的是，它们都要求恒定的熵率——即在输出的足够长子串中，需有恒定比例的标记，其经验熵至少为 O(log |T|)，其中 T 为标记的字母表；Golowich 等人还进一步要求 T 的规模大于安全参数。在本研究中，我们探讨当每个标记的熵仅为常数时，是否依然可以对 LLM 的输出加水印，从而消除对字母表大小或安全参数的依赖。在这一熵低的情形下，我们构建了： - 一种在随机替换下具有鲁棒性的水印方案（假设次指数级 LPN，参照 Christ 等人 [CRYPTO'24]）； - 一种在随机替换和随机删除下均鲁棒的水印方案，前提是满足附加的启发性假设——即 LLM 的输出仅引入随机错误（类似于 Christ 等人 [CRYPTO'24] 中的假设），或采用一种对抗任意替换和随机删除均鲁棒的伪随机纠错码构造。

## Abstract
A recent and exciting thread of work focuses on developing methods for watermarking the output of large language models (LLMs). We focus on provably undetectable watermarking-that is, schemes that do not alter the output distribution of the LLM, yet enable embedding a watermark in the output that identifies the output as having been generated by the particular LLM. Furthermore, the watermark should be hard to remove by an adversary that may potentially edit, insert, or delete tokens from the watermarked output. Indeed, recent work (Christ et al. [COLT'24], Christ et al. [CRYPTO'24], Golowich et al. [NeuroIPS'24]) shows how to develop such schemes that are robust against a constant fraction of substitutions, or even against a constant fraction of arbitrary edits.   These works, however, make strong assumptions on the entropy present in the output of the LLM. Most notably, they all require constant entropy rate-that is, a constant fraction of the tokens in a sufficiently long substring of the output need to have empirical entropy at least O(log |T|), where T is the alphabet of tokens, and Golowich et al. additionally require T to be larger than the security parameter. In this work, we consider whether we can also watermark the outputs of LLMs when the per-token entropy is just a constant, discarding the dependence on the alphabet size or security parameter. In this regime, we construct:   - A watermarking scheme robust against random substitutions (assuming subexponential LPN, as in Christ et al. [CRYPTO'24])   - A watermarking scheme robust against random substitutions and random deletions, given either the additional heuristic assumption that the output of the LLM only introduces random errors (analogous to the assumption made by Christ et al. [CRYPTO'24]) or a construction of a pseudorandom error-correcting code robust to adversarial substitutions and random deletions.

---

## 论文详细总结（自动生成）

# 《我们能否为低熵的大型语言模型输出加水印？》论文总结

---

## 一、研究核心问题与整体背景

- **研究动机**  
  随着大型语言模型（LLM）生成内容的广泛应用，区分 AI 生成内容与人类撰写内容成为重要问题。  
  为实现内容可溯源性，“水印”（watermarking）成为核心方向，即在不影响输出自然性的前提下，为 LLM 输出嵌入隐秘标识。  

- **问题核心**  
  现有可证明的不可检测水印方案（provably undetectable watermarking）通常依赖于 **高输出熵** 假设，即要求 LLM 输出中有较多具高多样性分布的 token。  
  自然语言往往以**低熵**输出居多（如固定格式回答、重复用词等），导致现有方案鲁棒性下降。  

- **本文目标**  
  探讨当 LLM 输出 **每个 token 的熵为常数甚至更低** 时，是否仍能实现具有：
  - **不可检测性（undetectability）**
  - **鲁棒性（robustness）**
  的水印机制。  

---

## 二、方法论与核心技术

### 1. 理论框架

- 基于 **伪随机纠错码（Pseudorandom Error-Correcting Codes, PRC）**。
- 核心思想：
  - 将水印比特序列嵌入 LLM 生成过程；
  - 利用 PRC 同时保证：
    - **伪随机性** → 输出分布不可区分；
    - **纠错性** → 抵御替换、删除等操作。

### 2. 技术创新点

- **低熵假设下的编码策略**  
  传统方法要求 token 具对数级别熵，本文仅需一部分 token 具至少 **1 bit 熵**；  
  通过将 token 空间随机哈希为二进制空间，转化为低维嵌入问题。

- **哈希映射嵌入机制**  
  - 对每个 token 取随机哈希函数 \( h: T \rightarrow \{0,1\} \)；
  - 在生成 token 时：
    1. 从模型分布中取两个样本 \( t_0, t_1 \)；
    2. 若 \( h(t_0) \neq h(t_1) \)，输出符合 PRC 比特的那个；
    3. 否则随机输出一个，造成独立小概率嵌入误差。
  - 该机制保持分布不变，实现不可检测性。

- **分块与重复哈希策略**
  - 输出序列被划分为若干块，每块使用独立的哈希函数；
  - 保证块间独立性以增强对删除攻击的鲁棒性。

### 3. 理论结果概述

- **Theorem 1–4（非正式）主要结论：**
  1. 若存在对抗替换鲁棒的二进制 PRC，可构造对任意大字母表的 LLM 实现：
      - 对 (1/2 − ε) 随机替换鲁棒；
      - 不可检测。
  2. 若 PRC 同时容忍随机删除 + 伪随机假设，则可鲁棒对付：
      - (1/2 − ε) 替换 + (1 − ε’) 删除。
  3. 若具可对抗替换 + 删除的 PRC，可无需启发性假设。
  4. 若使用 “permuted code” PRC（基于新假设），可扩展至少量任意编辑（insert/delete）。

- **算法流程（简化）：**
  1. `Gen`: 生成密钥与哈希函数组；
  2. `Watermark`: 按上述过程嵌入比特；
  3. `Detect`: 通过哈希重建比特并用 PRC 解码检测。

---

## 三、实验设计

- **性质**：本文为**理论性研究**，未进行传统意义上的数值或实证实验。  
  - 主要采用形式化证明与概率分析；
  - 无具体的自然语言数据集实验。
- **对比基线**：
  - [CGZ24]、[CG24]、[GM24] 等高熵水印方案；
  - 对比维度包括：
    - 所需熵水平；
    - 对替换、删除、编辑攻击的鲁棒范围；
    - 是否依赖启发性假设；
    - 加密假设（LPN、permuted codes 等）。

---

## 四、资源与算力

- 本文是一篇**理论密码学与信息安全论文**，未涉及模型训练或 GPU 运算。  
- 无任何实验算力、数据规模或时间成本说明。

---

## 五、实验数量与充分性

- 未进行实证实验，因此不存在多组实验或消融分析。  
- 理论分析覆盖多种攻击模型（随机 / 对抗 替换、删除、编辑），论证结构完整且公式推导严谨，  
  在**理论充分性**上较为完备，但**实际可行性待验证**。

---

## 六、主要结论与发现

1. **低熵 LLM 输出仍可被鲁棒地水印化。**
2. **无需改变输出分布即可实现不可检测性。**
3. **理论证明**：  
   在仅假设部分 token 具至少 1 bit 熵的条件下，  
   可抵御约半数比例的随机替换与几乎全部随机删除。  
4. **在额外假设（或使用更强PRC构造）下，可对抗有限比例的任意编辑攻击。**

---

## 七、方法亮点

- **突破高熵依赖**：首次在常数熵甚至低熵场景下实现鲁棒不可检测水印；
- **形式化定义完善**：对“随机替换”“随机删除”在大字母表下重新定义；
- **理论鲁棒性证明完整**：结合 LPN 和伪随机码假设提供形式安全保证；
- **结构模块化**：通过可组合的 PRC 框架，可替换不同复杂度假设或新码构造；
- **实用意义**：为自然语言低多样性输出（如模板化回复）提供理论可行的水印机制。

---

## 八、不足与局限

- **缺乏实证验证**：未在真实 LLM 或语言语料上测试，无法验证嵌入开销及鲁棒性效率。  
- **依赖强假设**：
  - 依赖次指数 LPN 或非标准“permuted code”假设；
  - 某些结果需额外“随机误差分布”启发假设。
- **可实现性问题**：
  - 哈希函数选取与块划分在实际模型中可能影响生成质量；
  - 未涉及对实际攻击（如语义改写或表示层扰动）的评估。
- **鲁棒性上限有限**：对任意编辑的防御比例极低（ϵ ≈ O(δ⁴)），在实践中可能不足。
- **计算效率未知**：探测与解码过程开销未作性能分析。

---

**（完）**
