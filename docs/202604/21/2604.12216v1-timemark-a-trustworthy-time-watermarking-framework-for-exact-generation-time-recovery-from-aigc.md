---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一种可精确恢复AIGC生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 10.0
evidence: 用于从AIGC中精确恢复生成时间的水印框架
tldr: 本文提出TimeMark框架，通过引入密码学技术和时间相关密钥，实现对AIGC生成时间的可信水印嵌入与精确恢复，避免统计特征可见性和伪造风险，理论与实验验证其在司法取证中的高可靠性与防伪性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有水印方法依赖统计信号，检测结果具有概率性且可被伪造，无法满足司法取证需求。
method: 提出一个结合密码学与时间依赖密钥的可信时间水印框架，采用双阶段编码与纠错机制实现可靠恢复。
result: "理论分析与实验表明，该框架可实现100%生成时间识别准确率，并抵抗用户与提供方的攻击。"
conclusion: TimeMark框架实现了可验证、准确、防伪的时间水印，为AIGC知识产权取证提供可靠技术基础。
---

## 摘要
大型语言模型（LLMs）在文本生成中的广泛应用，引发了日益增长的知识产权争议。数字水印技术可将元信息嵌入人工智能生成内容（AIGC）中，具有作为司法证据的潜力。然而，现有方法依赖于词元分布中的统计信号，导致检测结果具有固有的概率性，在多比特编码（例如时间戳）情况下可靠性降低。此外，这类方法会引入可检测的统计模式，使其易受伪造攻击，模型提供方也可能伪造任意水印。为解决这些问题，我们提出了可信水印（trustworthy watermark）的概念，可实现100%识别精度的可靠恢复，同时抵御用户端的统计攻击和提供方的伪造。我们特别关注用于司法证据的可信时间水印。该框架融合了密码学技术，在监管监督下将时间信息编码进时间相关的密钥，防止任意生成时间戳。水印载荷与时间解耦，对每个实例生成随机且不存储的比特序列，从而消除统计模式。为确保可验证性，我们设计了两阶段编码机制，并结合纠错编码，实现了理论上完美准确的生成时间恢复。理论分析与实验结果均表明，该框架满足司法证据对可靠性的要求，并为未来AIGC相关知识产权纠纷提供了切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# TimeMark：一种可精确恢复AIGC生成时间的可信时间水印框架 —— 中文总结

---

## 一、研究背景与核心问题

- **研究动机**  
  随着大型语言模型（LLMs）在生成式人工智能内容（AIGC）中的广泛应用，文本创作的版权与知识产权归属争议日益增加。传统电子证据强调“创作时间”的法律效力，而AIGC输出往往缺乏能证明真实生成时间的可信记录。  
  当前常见的**水印方法**通过在词元采样分布中嵌入统计偏差来标记生成内容，但存在几个关键问题：
  1. **检测结果概率性**：多比特信息（如时间戳）嵌入后识别率降低。
  2. **可被伪造**：统计结构可被逆向或模仿。
  3. **提供方可造假**：模型服务方可任意生成虚假时间戳。  

  这些缺陷使现有水印方案无法满足司法取证场景对真实性、可验证性、防篡改性的高标准要求。本文提出的 **TimeMark 框架**致力于实现**“可信时间水印”**：在AIGC中精确标记真实生成时刻，并能被第三方验证且无法伪造。

---

## 二、方法论与技术框架

### 1. 核心思想
- **目标**：满足“可信水印”三大条件：
  1. 生成时间可**100%精确恢复**；
  2. **防止提供方伪造时戳**；
  3. **抗用户端统计攻击**。  
- **创新思想**：将时间绑定到 **时间依赖密钥（time-dependent key）** 而非直接嵌入时间戳比特，结合密码学机制和纠错编码实现可靠验证。

### 2. 模型结构（TimeMark总体流程）
整个框架包括：
- 编码阶段（Encoding Model）  
- 解码与验证阶段（Decoding Model）  

#### (1) 编码阶段
- **时间离散化**：将时间划分为可配置窗口（如分钟级），每个窗口对应密钥 \( K_t \)。
- **密钥生成**：采用一条单向哈希链  
  \[
  K_t = H(K_{t-1})
  \]
  并由**硬件安全模块 (HSM)** 管理，防止服务提供方重建旧密钥，从而避免伪造早期时间签名。
- **随机载荷生成**：对每段生成任务生成随机比特序列 \( R \)，经纠错编码（BCH）扩展为码字 \( P = ECC(R) \)。
- **双阶段编码机制**：
  - **阶段 I**（前 α|P| tokens）：使用密钥 \( K_t \)、随机数 R、前缀哈希生成伪随机种子，决定词汇“绿名单”（greenlist）；
  - **阶段 II**（后续 tokens）：去除 R，仅基于 \( K_t \) 和上下文生成对应偏置。  

  词元采样中对绿名单或红名单施加轻微logit偏置 δ，实现隐秘嵌入而不影响文本流畅性。

#### (2) 解码与验证阶段
- **第 1 步：载荷解码**  
  - 对各候选时间窗口的密钥逐一尝试；
  - 从阶段 II token 中按投票法恢复码字 \( \hat{P} \)；
  - 经 ECC 解码得到随机序列 \( \hat{R} \)。
- **第 2 步：验证匹配**  
  - 用候选\( K_t \)与 \( \hat{R} \)重建阶段 I 的绿名单；
  - 计算匹配率 Score(K,R)，若 ≥ 阈值ϕ（例如0.65），则认定该时间键正确。
  - 不同密钥的匹配率在理论上产生极大区分度（真密钥≈0.92匹配，随机≈0.5）。

### 3. 理论依据
- BCH(63,10) 编码具备最小距离 27，可纠正最多 13 位错误，使原随机序列恢复概率近似 1。
- 理论分析表明：
  - 真实密钥匹配错误率 ≈ 10⁻⁴⁴；
  - 假密钥误通过率 ≈ 4.76×10⁻⁸；
  - 可实现近乎完美的识别精度。

---

## 三、实验设计

- **实验场景**：
  - 构造 20 条与创作/报告相关的 prompt；
  - 每个 prompt 生成两组文本：加水印与不加水印；
  - 共计 **800 组生成实例**。
- **模型与实现**：
  - 采用 *Qwen2.5-7B* 模型；
  - 参数设定：  
    - 随机序列长度 10 bits；  
    - BCH 扩展到 63 bits；  
    - 文本长度 ≥ 945 tokens；  
    - α = 5，偏置 δ = 2.5。  
- **验证方式**：对每篇文本，针对正确及相邻 4 个时间窗口共 5 个候选密钥进行检测。

---

## 四、资源与算力

- 论文仅说明实验使用 Qwen2.5-7B 生成文本，但**未提供**明确的算力规格（GPU 型号、数量或计算时长等）。  
  可推断实验主要为推理阶段嵌码与解码过程，未涉及模型再训练，算力需求较低。

---

## 五、实验数量与充分性

- 共计 800 次生成与检测实验；每次包括带水印与不带水印对照，覆盖多主题 Prompt。
- 对验证阈值、解码步骤、理论概率均进行了详细分析。  
- 虽然实验场景较单一（仅长文本生成），但实验数量充足、统计验证标准清晰，结果具有强说服力。

---

## 六、主要结论与发现

- **可靠性**：在 800 次实验中，生成时间恢复准确率 **100%**。
- **假阳率**：无水印文本被误识别为带时间水印的比例 **0%**。
- **验证指标分离明显**：
  - 加水印文本匹配分数 ≈ 0.77；
  - 未加水印文本匹配分数 ≈ 0.50。  
- **理论与实验均证实**：设计实现完全的时间可恢复性、防伪性与抗统计攻击性，符合司法证据的真实性、完整性、可验证性要求。

---

## 七、方法优点与亮点

- **创新安全结构**：
  - 将时间信息与水印载荷完全解耦；
  - 设计时间链密钥 + 随机载荷机制，有效防止提供方和用户伪造。
- **统计隐蔽性高**：每次生成使用独立随机比特序列，不存在固定统计模式。
- **可公证审计**：密钥由外部HSM保管，具备司法可验证的合规性。
- **理论完备性强**：结合BCH码分析和概率推导，精确描述错误界。
- **检测简易**：无须访问模型参数，算法透明、可公开验证。

---

## 八、不足与局限

- **适用性**：当前方案仅在**长文本**条件下保持完美精度，对短文本或高编辑率内容尚不适应。
- **鲁棒性限制**：假设用户不修改生成内容，未完全解决后期编辑、 paraphrase 等攻击。
- **通用性验证不足**：仅对单模型(Qwen2.5-7B)验证，未测试跨模型、跨语言场景。
- **资源与性能指标缺失**：无详细计算消耗与效率对比，难以评估大规模部署可行性。
- **未对比其他水印算法实测**：仅理论上与现有LLM水印方案对比，缺少直接benchmark实验。

---

**（完）**
