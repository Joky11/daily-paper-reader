---
title: Can we Watermark Low-Entropy LLM Outputs?
title_zh: 我们能为低熵的大型语言模型输出添加水印吗？
authors: "Noam Mazor, Andrew Morgan, Rafael Pass"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12051v1"
tags: ["query:q8"]
score: 9.0
evidence: 讨论了LLM输出的可证明不可检测水印技术及其对抗编辑的鲁棒性。
tldr: 本文研究低熵情况下大型语言模型输出的可水印性，挑战现有依赖高熵假设的水印方法。作者提出两种新构造：一种在次指数LPN假设下能抗随机替换，另一种在附加假设或伪随机纠错码支持下能同时抗随机替换与删除，从而扩展了水印技术的适用范围。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12051v1/fig-001.webp\", \"caption\": \"Figure 1: Summary of our results compared to related results in terms of per-token entropy requirement (over a sufficiently long substring unless otherwise stated); robustness to (random or adversarial) substitutions, insertions, and deletions; and required assumptions.\", \"page\": 6, \"index\": 1, \"width\": 958, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-002.webp\", \"caption\": \"Figure 2: Protocol Gen for generating the secret information used in the watermarking protocol.\", \"page\": 19, \"index\": 2, \"width\": 948, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-003.webp\", \"caption\": \"Figure 3: Protocol Watermark for watermarking a generative model.\", \"page\": 20, \"index\": 3, \"width\": 948, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-004.webp\", \"caption\": \"Figure 4: Protocol Detect for detecting the presence of a watermark in a given message.\", \"page\": 21, \"index\": 4, \"width\": 948, \"height\": 327}]"
motivation: 现有水印方案依赖高熵输出，限制了在低熵LLM生成文本中的应用。
method: 作者基于次指数LPN假设和伪随机纠错码设计鲁棒水印方案，对随机替换及删除进行抵抗。
result: 本文提出能在低熵输出情况下为大型语言模型生成内容添加水印的方案，包括对随机替换和删除均具鲁棒性的构造。
conclusion: 研究表明，即使输出熵较低，也能通过新的密码学构造实现鲁棒的不可检测水印。
---

## 摘要
近期一个令人兴奋的研究方向聚焦于开发大型语言模型（LLMs）输出结果的水印方法。我们关注的是可被形式化证明为不可检测的水印——也就是说，这类方案不会改变 LLM 的输出分布，但可以在输出中嵌入水印，以标识该输出是由特定的 LLM 生成的。此外，该水印应当难以被潜在的对手移除，即使对手可以编辑、插入或删除水印输出中的 token。实际上，近期的研究（Christ 等 [COLT'24]，Christ 等 [CRYPTO'24]，Golowich 等 [NeurIPS'24]）展示了如何设计出能够抵御恒定比例替换，甚至是恒定比例任意编辑的鲁棒方案。然而，这些工作普遍对 LLM 输出中的熵作出了较强假设。尤其是，它们都要求恒定熵率——即在输出的一个足够长的子串中，有恒定比例的 token 经验熵至少为 O(log |T|)，其中 T 为 token 的字母表。此外，Golowich 等还要求 T 的大小必须大于安全参数。在本研究中，我们探讨当每个 token 的熵仅为常数时（不依赖于字母表大小或安全参数），是否仍然可以对 LLM 的输出进行水印。在这一设定下，我们构建了：  - 一个可在随机替换下保持鲁棒的水印方案（基于与 Christ 等 [CRYPTO'24] 相同的次指数 LPN 假设）；  - 一个可在随机替换与随机删除下保持鲁棒的水印方案，前提是假设 LLM 的输出仅引入随机误差（类似于 Christ 等 [CRYPTO'24] 的启发式假设），或假设存在一种能抵御对抗性替换与随机删除的伪随机纠错码。

## Abstract
A recent and exciting thread of work focuses on developing methods for watermarking the output of large language models (LLMs). We focus on provably undetectable watermarking-that is, schemes that do not alter the output distribution of the LLM, yet enable embedding a watermark in the output that identifies the output as having been generated by the particular LLM. Furthermore, the watermark should be hard to remove by an adversary that may potentially edit, insert, or delete tokens from the watermarked output. Indeed, recent work (Christ et al. [COLT'24], Christ et al. [CRYPTO'24], Golowich et al. [NeuroIPS'24]) shows how to develop such schemes that are robust against a constant fraction of substitutions, or even against a constant fraction of arbitrary edits.   These works, however, make strong assumptions on the entropy present in the output of the LLM. Most notably, they all require constant entropy rate-that is, a constant fraction of the tokens in a sufficiently long substring of the output need to have empirical entropy at least O(log |T|), where T is the alphabet of tokens, and Golowich et al. additionally require T to be larger than the security parameter. In this work, we consider whether we can also watermark the outputs of LLMs when the per-token entropy is just a constant, discarding the dependence on the alphabet size or security parameter. In this regime, we construct:   - A watermarking scheme robust against random substitutions (assuming subexponential LPN, as in Christ et al. [CRYPTO'24])   - A watermarking scheme robust against random substitutions and random deletions, given either the additional heuristic assumption that the output of the LLM only introduces random errors (analogous to the assumption made by Christ et al. [CRYPTO'24]) or a construction of a pseudorandom error-correcting code robust to adversarial substitutions and random deletions.

---

## 论文详细总结（自动生成）

# 《Can we Watermark Low-Entropy LLM Outputs?》论文总结

---

## 一、研究动机与核心问题

- **研究背景**：  
  随着大型语言模型（LLMs）广泛应用于内容生成，如何区分“AI生成”与“人类生成”的文本成为关键问题。水印技术（在生成过程中嵌入可检测的标识）是当前主流解决方案之一。  
  然而，许多现有方案在理论上要求**高熵输出**（即生成文本在token层面的多样性较大），而自然语言中往往大部分token熵较低（如常用词、标点等）。这种高熵假设使得现有的“不可检测水印”方案难以应用于真实场景。

- **研究核心问题**：  
  当LLM输出的**每个token熵仅为常数（低熵）**时，是否还存在一种**既不可检测又对编辑鲁棒**的水印方案？

- **研究目标**：  
  1. 消除对token字母表大小|T|或安全参数λ的熵依赖。  
  2. 设计可在低熵条件下仍能抵御随机替换/删除的水印方法。  
  3. 保持水印方案的形式化不可检测性（computational indistinguishability）。

---

## 二、方法论与技术细节

### 1. 核心思想

- 基于**伪随机纠错码（PRC, Pseudorandom Code）**框架，将水印嵌入到语言模型输出的随机性中，而不改变输出分布。  
- 提出一种适用于**低熵token空间**的水印嵌入策略，利用哈希函数将大字母表token空间映射至二进制，用PRC来提供鲁棒性与不可区分性。

### 2. 关键技术路线

1. **哈希映射**：  
   - 定义随机哈希函数 \( h: T \rightarrow \{0,1\} \)，将每个token映射为单比特。  
   - 保证即便token空间庞大（如数万词汇），每个token在哈希空间的贡献保持恒定熵。

2. **嵌入算法（Watermark步骤）**：  
   - 生成两个独立样本 \( t_0, t_1 \) 来模拟语言模型的下一个token分布。  
   - 如果 \( h(t_0) \neq h(t_1) \)，则选择与PRC码字当前位\( c_i \)匹配的token；  
   - 否则随机选择两者之一，这种情况下会引入随机嵌入错误。

3. **块级哈希复用方案**：  
   - 输出被划分为多个块，每个块对应一个PRC码字。  
   - 为每个块生成独立的哈希函数 \( h_j \)；保证在删除攻击中仍能对齐解码。

4. **检测算法（Detect步骤）**：  
   - 对输入序列每个子串通过 \( h \) 哈希化后，尝试恢复PRC码字。  
   - 若能成功解码（返回非⊥结果），即可检测出嵌入水印。

### 3. 理论结果（主要定理）

- **Theorem 1 / Corollary 1**：  
  在次指数LPN假设下，存在对**随机替换鲁棒**、**不可检测**的低熵水印方案。

- **Theorem 2 / Corollary 2**：  
  若假设LLM输出误差为随机（“自然提示”假设），或存在对抗性替换+随机删除鲁棒的PRC，则方案可同时抵抗随机替换与随机删除。

- **Theorem 3–4**：  
  若存在可抵抗对抗性编辑的PRC（基于“permuted codes”假设），则可实现对常数比例对抗性编辑鲁棒的低熵水印。

---

## 三、实验设计与验证场景

- **说明**：  
  论文为理论性研究，重点在构造与安全证明，**未进行具体的实证实验**。  
  文中所有结果通过**概率分析与理论证明**给出，不依赖于具体的LLM模型或数据集。

- **验证方式**：  
  通过形式化定义与证明展示方案满足：
  1. **不可检测性**：水印嵌入不改变生成分布；  
  2. **鲁棒性**：在随机替换/删除通道后仍可正确检测。

---

## 四、资源与算力说明

- 文中**未提及具体计算资源或实验算力**。  
  因该研究完全基于理论建模与密码学证明，不需要GPU训练或大规模实验计算。

---

## 五、实验数量与充分性

- **实验形式**：  
  无实测实验或benchmark比较，仅以**理论证明、定理推导、渐近复杂度分析**支持结论。

- **充分性分析**：  
  - 理论分析完整，涵盖不同攻击渠道（替换、删除、编辑）。  
  - 然而，缺乏在真实文本或实际LLM生成中的验证，因此**经验充分性不足**。

---

## 六、主要结论与发现

1. **低熵可水印性首次被形式化证明**：  
   即便LLM输出熵极低（部分token仅1 bit熵），仍可构造鲁棒水印方案。

2. **提出多层鲁棒水印构造**：  
   - 随机替换鲁棒（Theorem 1）；  
   - 随机替换+删除鲁棒（Theorem 2）；  
   - 对抗性编辑鲁棒（Theorem 3–4）。

3. **维持可计算不可检测性**：  
   所有构造均保证输出在统计意义上与未加水印输出不可区分。

4. **理论与实际兼容性更高**：  
   方案直接作用于token级输出，不依赖二进制编码转换，更贴近自然语言处理任务。

---

## 七、优点

- **突破低熵限制**：首次在常数熵场景实现形式可证明的鲁棒水印。  
- **无偏输出优势**：嵌入过程不改变LLM的样本分布。  
- **通用性强**：兼容任意大token字母表（适合自然语言tokenizer）。  
- **安全性分析全面**：对替换、删除、编辑多通道均给出理论鲁棒性界限。  
- **与现有PRC体系兼容**：可直接基于已知的伪随机码（如基于LPN的构造）。

---

## 八、不足与局限

- **缺乏实测评估**：未在真实LLM输出上验证水印可检测率或误检率。  
- **依赖强假设**：
  - 次指数LPN（Learning Parity with Noise）难题；
  - “natural prompt”假设可能不符合自然语言分布。
- **编辑鲁棒性受限**：依靠“permuted codes”假设时，抗编辑比例较小（ϵ为极小常数）。  
- **实际攻防评估不足**：尚未考虑语义等价编辑（如同义替换）的实际检测表现。  
- **工程可实现性问题**：方案涉及哈希随机化与块级重编码，推理部署复杂度高。

---

**（完）**
