---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一个用于从AIGC精确恢复生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于验证 AIGC 生成时间的水印框架
tldr: 本文针对大模型生成文本中水印不可靠的问题，提出TimeMark框架，通过引入加密技术和两阶段编码，实现可验证的时间水印，使生成时间可精确恢复并具备法律证明的可信度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有文本生成水印方法检测具有概率性且易被伪造，无法作为可靠司法证据。
method: 提出一个结合加密技术的可信时间水印框架，通过时间依赖的密钥编码生成时间信息并采用两阶段编码和纠错机制。
result: 理论与实验均表明该框架可实现生成时间的完全准确恢复并防御伪造与攻击。
conclusion: TimeMark为解决AIGC知识产权争议提供了可行且可信的司法证据方案。
---

## 摘要
大型语言模型（LLMs）在文本生成中的广泛应用引发了越来越多的知识产权争议。水印技术通过将元信息嵌入到人工智能生成内容（AIGC）中，有潜力作为司法证据。然而，现有方法依赖于词元分布中的统计信号，导致检测结果具有内在的概率性，尤其在多比特编码（例如时间戳）情况下可靠性降低。此外，这些方法引入可检测的统计模式，使其易受伪造攻击，并使模型提供者能够任意伪造水印。为解决这些问题，我们提出了“可信水印”的概念，该方法在抵御用户端统计攻击和提供者端伪造的同时，能够以100%的识别准确率实现可靠恢复。我们重点研究用于司法证据的可信时间水印。我们的框架结合了密码学技术，并在监管监督下将时间信息编码到与时间相关的秘密密钥中，以防止任意时间戳的伪造。水印载荷与时间解耦，为每个实例生成随机的、不存储的比特序列，从而消除了统计模式。为确保可验证性，我们设计了一个两阶段编码机制，并结合纠错码，使生成时间能够以理论上完美的准确率可靠恢复。理论分析和实验结果表明，我们的框架满足司法证据的可靠性要求，并为未来与AIGC相关的知识产权争议提供了一种切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# TimeMark：用于从AIGC恢复生成时间的可信时间水印框架 — 深度总结

---

## 一、核心问题与研究背景

- **研究动机**：随着大型语言模型（LLMs）在创意写作、研究报告等高价值内容生成中的广泛应用，**知识产权归属与生成时间的判定**成为司法争议中的核心问题。传统电子证据常依赖数字时间戳或文件版本历史，但LLM生成内容通常只记录发布时间，而**缺乏可信的“生成时间”记录**。

- **问题现状**：现有LLM水印技术多通过在词元分布中嵌入统计偏差来识别是否由模型生成，但：
  - 检测结果具有概率性，无法保证100%正确；
  - 多比特水印（如时间戳编码）恢复精度显著下降；
  - 存在可识别的统计模式，易遭受**伪造攻击**；
  - 模型提供者可通过控制编码机制伪造任意时间戳。

- **研究目标**：设计一种**司法级可信水印框架**，能以**理论上精准的生成时间恢复**作为知识产权证据，且**防御提供者侧与用户侧的伪造行为**。

---

## 二、方法论与技术框架

### 核心思想
提出 **TimeMark 框架**：一个结合密码学与语言模型水印的系统，旨在实现**100%可靠的时间恢复**与防伪。  
关键创新为：
- **将时间信息绑定到时间依赖的秘密密钥**而非直接作为水印载荷；
- **水印载荷为每次随机生成的非存储比特序列**，消除固定统计特征；
- **设计两阶段编码机制**实现随机载荷的可信验证；
- **引入纠错码（BCC/BCH）**确保水印可完美恢复。

### 技术流程
1. **时间密钥生成**：
   - 时间被划分为离散窗口 \(t\)；
   - 每个时间窗口的密钥通过单向哈希链生成：  
     \[
     K_t = H(K_{t-1})
     \]
     由硬件安全模块（HSM）监管存储，防止提供者回溯伪造旧时间戳。

2. **水印编码阶段**：
   - 随机生成比特序列 \(R \in \{0,1\}^m\)，并经纠错码扩展：
     \[
     P = ECC(R)
     \]
   - 位置分配函数 \(A(i)\) 定义每个词元对应的水印位。
   - **两阶段编码机制**：
     - **阶段 I**：前半部分词元使用密钥\(K_t\)、随机序列\(R\) 和前缀哈希生成种子；
     - **阶段 II**：后半部分编码不再使用\(R\)，仅依赖密钥\(K_t\)，用于验证。

3. **解码与验证过程**：
   - **步骤一（恢复）**：针对多个候选时间窗口，利用\(K_t\)重建第二阶段种子并多数投票恢复\(P\)，再经 ECC 解码得 \(R\)；
   - **步骤二（验证）**：用恢复的 \(R\) 与\(K_t\)重建第一阶段水印分区，计算匹配度：
     \[
     Score(K_t, \hat{R}) = \frac{1}{\alpha|P|}\sum_{i=1}^{\alpha|P|} Match(i)
     \]
     若得分 ≥ 阈值 \(ϕ\)，则判定生成时间为对应 \(t\)。

4. **防伪性原理**：
   - 历史密钥仅由 HSM监管，服务方无法伪造；
   - 随机载荷打破统计依赖，防止用户端训练伪造模型；
   - 两阶段验证及 BCH 编码保证理论上零假阳性。

---

## 三、实验设计

- **实验场景**：
  - 从20个不同主题的创意与报告类 prompt 中随机抽样任务；
  - 每次任务生成两份文本：带水印与自然生成版本；
  - 共进行 **800 次独立试验**，用于评估检测准确性与验证强度。

- **模型与环境**：
  - 使用 **Qwen2.5-7B** 语言模型（无训练修改）；
  - 时间密钥候选集合包含**真实时间及其相邻4个窗口**；
  - 水印参数：  
    - payload 长度：63位 BCH 编码（由10位随机序列扩展）；  
    - 验证阈值 \(ϕ = 0.65\)、文本长度 ≥ 945 tokens、阶段比例 α = 5。

- **对比方法**：
  - 与普通无水印编码结果比较；
  - 未对比其他外部水印算法，仅验证准确度与防伪性（重点为司法可信度而非性能benchmark）。

---

## 四、资源与算力

- 论文中**未详细说明硬件或GPU配置**。  
  仅提到实验使用公开模型 Qwen2.5-7B，并未涉及显卡型号、数量或运行时间。  
  因此无法评估算力需求。

---

## 五、实验数量与充分性

- 共计 **800 组生成任务实验**；
- 每组进行**5个时间候选键验证**；
- 从准确性与防伪检测角度实验较充分，覆盖不同文本场景；
- 但未包含**编辑鲁棒性／跨模型比较／消融实验**，局限于一模型、一场景。

---

## 六、主要结论与发现

- TimeMark 框架实现了：
  - **生成时间恢复的100%准确率**；
  - **零错误识别率**（非水印文本均被正确判定为无时间水印）；
  - **显著分离度**：验证得分水印文本均值0.7699，对照文本均值0.4993；
  - 理论分析显示：
    - 误拒概率 < 10⁻⁴⁴；
    - 误接受概率 ≈ 4.76 × 10⁻⁸；
  ⇒ 几乎达到司法意义上的“确定性证据”标准。

---

## 七、优点与亮点

- **理论完备**：将法律证据要求系统化映射为五项技术要求（真实性、可靠性、防篡改性、可验证性、常规生成性）。
- **创新设计**：
  - 时间密钥与载荷解耦；
  - 硬件安全模块（HSM）引入监管保证；
  - 两阶段编码与随机载荷结合实现可验证随机性；
  - BCH纠错保障完美恢复。
- **实证充足**：实验与理论分析一致，误差界极低。
- **司法适用性**：框架可直接嵌入云服务操作流程，形成常规生成证据。

---

## 八、不足与局限

- **文本长度依赖**：仅支持长文本（≥945 token），短文不保证识别率；
- **修改敏感性**：假定文本未被编辑、改写或删除 token；
- **模型单一**：实验仅在 Qwen2.5-7B 上验证，缺乏跨模型与跨语言实验；
- **无算力报告**：未提供实现复杂度与推理开销分析；
- **应用局限**：尚未考虑语义级水印或无监督生成场景的兼容性。

---

（完）
