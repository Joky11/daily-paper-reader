---
title: Can we Watermark Low-Entropy LLM Outputs?
title_zh: 我们能否给低熵的大语言模型输出添加水印？
authors: "Noam Mazor, Andrew Morgan, Rafael Pass"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12051v1"
tags: ["query:q8"]
score: 9.0
evidence: 针对大语言模型输出的水印技术，用于身份识别并具备抗编辑鲁棒性
tldr: 本文探讨低熵环境下的大语言模型输出水印问题，突破现有方案对高熵输出的依赖，提出两种新型鲁棒水印机制：一种基于LPN假设可抵抗随机替换，另一种在额外假设或伪随机纠错码支持下可抵抗替换与删除。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12051v1/fig-001.webp\", \"caption\": \"Figure 1: Summary of our results compared to related results in terms of per-token entropy requirement (over a sufficiently long substring unless otherwise stated); robustness to (random or adversarial) substitutions, insertions, and deletions; and required assumptions.\", \"page\": 6, \"index\": 1, \"width\": 958, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-002.webp\", \"caption\": \"Figure 2: Protocol Gen for generating the secret information used in the watermarking protocol.\", \"page\": 19, \"index\": 2, \"width\": 948, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-003.webp\", \"caption\": \"Figure 3: Protocol Watermark for watermarking a generative model.\", \"page\": 20, \"index\": 3, \"width\": 948, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2604.12051v1/fig-004.webp\", \"caption\": \"Figure 4: Protocol Detect for detecting the presence of a watermark in a given message.\", \"page\": 21, \"index\": 4, \"width\": 948, \"height\": 327}]"
motivation: 现有水印方案依赖输出拥有高熵率，而实际LLM常产生低熵输出，需研究能在此情况下仍可水印的技术。
method: 论文构建了两种在低熵场景下仍可实现的LLM输出水印方案。
result: 在常数熵率下，作者提出基于LPN假设的随机替换鲁棒水印方案，以及在额外假设下应对替换和删除的水印方案。
conclusion: 研究表明在常数熵率下仍能实现具有鲁棒性的LLM输出水印，为低熵文本的水印设计提供理论与方法支撑。
---

## 摘要
近期的研究热点之一是开发用于大语言模型（LLM）输出的水印技术。本文聚焦于“可证明不可检测”的水印方法——即不改变LLM输出分布的方案，但仍能在输出中嵌入水印，以识别该输出是由特定LLM生成的。此外，该水印应难以被试图通过编辑、插入或删除词元来移除水印的对手所破坏。已有工作（Christ 等人 [COLT'24]、Christ 等人 [CRYPTO'24]、Golowich 等人 [NeurIPS'24]）展示了如何设计此类方案，使其对恒定比例的替换，甚至任意编辑具有鲁棒性。然而，这些工作都对LLM输出中的熵做出了较强的假设。尤为重要的是，它们要求恒定的熵率——即在输出的足够长子串中，一定比例的词元须具有至少 O(log |T|) 的经验熵，其中 T 表示词元字母表，Golowich 等人还要求 T 的规模大于安全参数。本文探讨当每个词元的熵仅为常数、且不依赖于字母表大小或安全参数时，能否对LLM的低熵输出进行水印。在该低熵条件下，我们构造了如下方案：  - 一种在假设次指数级学习噪声问题（LPN）的前提下，对随机替换具有鲁棒性的水印方案（与 Christ 等人 [CRYPTO'24] 中相同假设）；  - 一种对随机替换和随机删除均具鲁棒性的水印方案，前提是附加启发式假设——LLM输出仅引入随机错误（与 Christ 等人 [CRYPTO'24] 相似），或构造一种对抗性替换和随机删除均鲁棒的伪随机纠错码。

## Abstract
A recent and exciting thread of work focuses on developing methods for watermarking the output of large language models (LLMs). We focus on provably undetectable watermarking-that is, schemes that do not alter the output distribution of the LLM, yet enable embedding a watermark in the output that identifies the output as having been generated by the particular LLM. Furthermore, the watermark should be hard to remove by an adversary that may potentially edit, insert, or delete tokens from the watermarked output. Indeed, recent work (Christ et al. [COLT'24], Christ et al. [CRYPTO'24], Golowich et al. [NeuroIPS'24]) shows how to develop such schemes that are robust against a constant fraction of substitutions, or even against a constant fraction of arbitrary edits.   These works, however, make strong assumptions on the entropy present in the output of the LLM. Most notably, they all require constant entropy rate-that is, a constant fraction of the tokens in a sufficiently long substring of the output need to have empirical entropy at least O(log |T|), where T is the alphabet of tokens, and Golowich et al. additionally require T to be larger than the security parameter. In this work, we consider whether we can also watermark the outputs of LLMs when the per-token entropy is just a constant, discarding the dependence on the alphabet size or security parameter. In this regime, we construct:   - A watermarking scheme robust against random substitutions (assuming subexponential LPN, as in Christ et al. [CRYPTO'24])   - A watermarking scheme robust against random substitutions and random deletions, given either the additional heuristic assumption that the output of the LLM only introduces random errors (analogous to the assumption made by Christ et al. [CRYPTO'24]) or a construction of a pseudorandom error-correcting code robust to adversarial substitutions and random deletions.

---

## 论文详细总结（自动生成）

# 我们能否给低熵的大语言模型输出添加水印？——论文总结  

---

## 一、核心问题与研究动机

- **研究背景**：  
  当前大语言模型（LLMs）的输出在现实应用中与人类生成文本越来越难以区分，这引发了监管与溯源需求。水印技术被视为解决 AI 内容可溯源问题的关键方案。  

- **主要问题**：  
  现有“可证明不可检测”（provably undetectable）的水印方案大多依赖于**高熵输出假设**——即要求 LLM 输出的词元分布在足够大的字母表上拥有较高的经验熵（如 O(log|T|)）。然而，现实中的文本往往是低熵的，例如生成自然语言或固定语式时，符号分布集中，难以满足高熵条件。  

- **研究目标**：  
  本文探讨 **是否能在低熵场景下实现同时具备不可检测性与鲁棒性** 的 LLM 水印机制，使得水印在仅常数级熵下仍能嵌入且抵抗编辑、替换与删除攻击。  

---

## 二、方法论与技术路线

### （1）总体思路
- 以 **伪随机纠错码（Pseudorandom Codes, PRCs）** 为核心，将其作为水印信息的载体。
- 通过嵌入伪随机码字，使水印既对应于加密随机性（保证不可检测性），又具纠错性（保证鲁棒性）。
- 与先前的 [CG24], [GM24] 等方法相比，作者突破了对高熵的需求，仅要求常数熵水平。

### （2）技术框架
1. **随机哈希映射机制**：  
   - 将 LLM 生成的每个词元通过随机哈希函数映射到一个二值空间 `{0,1}`，从而将高维词汇表转化为可嵌入的二进制位。
   - 每一段文本的词元使用同一个哈希函数，以便在删除或插入时可按区块恢复。

2. **嵌入算法（Watermark）流程**：  
   - 对每个区块生成一段伪随机码字 `c`。  
   - 对于每个词元候选分布 D，采样两个候选 `t₀,t₁`。  
   - 若 `h(t₀) ≠ h(t₁)`，输出与 `c_i` 对应哈希值的那个词元（“成功”嵌入）；若相同则随机选一个（“失败”）。  
   —这种双采样策略保证不会偏离原分布。  

3. **检测算法（Detect）流程**：  
   - 使用同一哈希函数组，对生成文本的各词元进行哈希。  
   - 在每个长度为 ℓ 的区块内运行 PRC 解码算法，若可恢复有效码字则判定存在水印。  

4. **理论分析重点**：  
   - 通过概率和浓度边界（如 Hoeffding 不等式）证明，只要常数比例词元的经验熵 ≥ 1，比特嵌入成功率即可超过 1/2。
   - 基于 **次指数级 LPN（Learning Parity with Noise）假设**，保证 PRC 在 1/2−ε 随机替换下仍可恢复。
   - 扩展分析包括随机替换 + 随机删除，以及对抗性编辑（通过采用 [CGG+25] 的“permuted codes”假设）。  

---

## 三、实验设计与对比

- **实验特征**：  
  论文属于**理论研究与算法构造**，未进行具体的数值实验或数据集测试。  
  全文以数学推导、概率分析和构造性证明为主，不涉及实际模型推理或水印检测实验。  

- **对比方法**：  
  在理论层面对比了多种既有方案：
  - [CG24]：基于常数熵的二元水印，需次指数级 LPN 假设。  
  - [GM24]：高熵（O(log|T|)) 方案，需多项式大字母表。  
  - [CGZ24]：可检测但仅对未修改的长子串有效。  
  - [CGG+25]：使用“permuted codes”获得对抗性编辑鲁棒性。  
  本文在表格（Figure 1）中系统比较了熵要求与鲁棒范围。  

---

## 四、资源与算力使用

- **说明**：  
  本文完全基于理论分析与复杂性假设，没有执行计算实验。  
  因此文中未涉及 GPU 型号、数量或训练时间等计算资源信息。  

---

## 五、实验数量与充分性

- **实验性质**：理论推导，无独立实验组。  
- **充分性说明**：  
  作者通过多级理论推理（四个主要定理与多个非正式定理）涵盖多种鲁棒性形式——随机替换、随机删除、对抗性编辑。  
  虽然无实证验证，但在理论证明层面覆盖相当充分，符合密码学论文的标准做法。  

---

## 六、主要结论与发现

- 在仅需**常数级熵率**的条件下，仍可实现：
  1. **随机替换鲁棒水印方案**（基于 LPN 假设）：  
     对 1/2−ε 比例随机替换保持检测能力。  
  2. **随机删除 + 替换鲁棒水印方案**：  
     在附加“自然提示（Natural Prompt）”假设或加强型 PRC 下可行。  
  3. **对抗性编辑鲁棒方案**：  
     结合 [CGG+25] 的“permuted codes”假设，证明了在常数比特熵条件下依然可检测。  

- **核心发现**：  
  低熵输出并不必然阻止水印嵌入，只需适当的伪随机编码和分块哈希机制即可维持鲁棒性能。  

---

## 七、优点与创新点

- **理论突破**：  
  - 证明首次在常数级熵条件下实现可证明鲁棒水印。  
  - 不再依赖字母表大小或安全参数的对数量级熵。  

- **结构创新**：  
  - 使用分块哈希与双采样嵌入策略在不偏离模型分布的同时嵌入水印。  
  - 保留 LLM 输出在词元层面的自然结构，更贴合实际攻击场景（如替词、删词）。  

- **鲁棒性广度**：  
  - 覆盖随机与对抗性替换、删除、编辑三类攻击。  
  - 减少对启发式假设的依赖，并明确指出如何可通过更强 PRC 结构规避。  

---

## 八、不足与局限

- **缺乏实证验证**：  
  无实际实现或在真实 LLM 输出上的性能测试，难以评估嵌入效率或检测精度。  

- **启发式假设仍存在**：  
  如对“自然提示”或“输出误差可视为独立随机”的假设在真实文本上难以验证。  

- **复杂度与实用性问题**：  
  伪随机纠错码与哈希方案在实际数十亿词元的生成中可能带来计算与存储开销。  

- **鲁棒参数较弱**：  
  对抗性编辑的鲁棒度常数较小（~O(δ⁴)），实际有效范围有限。  

---

**（完）**
