---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一种可从 AIGC 精确恢复生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于AIGC精确生成时间恢复和司法证据的水印技术
tldr: 本文提出TimeMark，一个可信时间水印框架，通过加密技术与时间相关密钥结合实现生成时间精确恢复，避免传统方案的统计弱点与伪造风险，为AIGC版权与司法取证提供可靠解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有LLM水印方法依赖统计信号检测，可靠性低且易被伪造，无法提供司法可信证据。
method: 提出TimeMark框架，将加密技术与时间相关密钥结合，通过双阶段编码和纠错码实现可验证的水印嵌入。
result: "理论与实验证明TimeMark可实现100%生成时间准确恢复并抵抗伪造与统计攻击。"
conclusion: TimeMark满足司法证据的可靠性要求，为AIGC知识产权争议提供可行的技术支撑。
---

## 摘要
大型语言模型（LLMs）在文本生成中的广泛使用引发了越来越多的知识产权争议。数字水印技术通过将元信息嵌入人工智能生成内容（AIGC）中，有潜力作为司法证据。然而，现有方法依赖于 token 分布中的统计信号，导致检测结果具有固有的概率性，在多比特编码（如时间戳）情况下可靠性下降。此外，这些方法会引入可检测的统计模式，使其易受伪造攻击，并使模型提供者能够生成任意水印。为了解决这些问题，我们提出了“可信水印”（trustworthy watermark）的概念，它能够以 100% 的识别精度实现可靠恢复，同时抵御用户侧的统计攻击和提供者侧的伪造。我们重点研究可作为司法证据使用的可信时间水印。该框架将密码学技术与时间信息相结合，将时间信息编码到受监管监督的时间相关密钥中，从而防止任意时间戳伪造。水印负载与时间解耦，每个实例生成随机且不存储的比特序列，消除统计模式。为确保可验证性，我们设计了两阶段编码机制，并结合纠错码，使得生成时间的可靠恢复在理论上达到完美精度。理论分析和实验结果均表明，我们的框架满足司法证据的可靠性要求，并为解决未来 AIGC 相关的知识产权争议提供了切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# TimeMark：一种可从 AIGC 精确恢复生成时间的可信时间水印框架 — 中文结构化总结

---

## 一、核心问题与研究背景

- **问题动机**：  
  随着大型语言模型（LLM）广泛用于生成文字、报告等高价值内容，关于 AIGC（AI-Generated Content）的**知识产权归属与版权争议**不断增加。  
  传统的数字时间戳仅能记录**发布时间**，而非**真实生成时间**，在司法取证中缺乏可信性。  

- **现有技术的不足**：  
  - 现有 LLM 水印方案依赖 token 分布的统计偏差进行检测，具有概率性，无法实现 100% 准确的时间恢复。  
  - 多比特水印（例如时间戳）检测可靠性显著下降，容易被用户通过统计观察和模型提供者通过伪造时间戳攻击。  
  - 模型提供方可随意生成任意时间的“伪水印”，无法用于司法证据。

- **研究目标**：  
  本文提出**可信时间水印（Trustworthy Time Watermark）**概念，旨在构建一个可用于司法、可靠且可验证的生成时间证明机制，确保生成时间可**精确恢复且不可伪造**。

---

## 二、方法论与核心技术

### 2.1 核心思想

TimeMark 框架将时间水印与密码学机制结合，提出“**可信水印三要素**”：  
1. **100% 时间识别准确性**；  
2. **防止提供方伪造任意时间戳（provider-side unforgeability）**；  
3. **防止用户侧统计伪造（user-side unforgeability）**。

### 2.2 技术架构概要

- **时间相关密钥链（Time-Dependent Key Chain）**  
  - 每个时间窗口 \(t\) 生成一个密钥 \(K_t = H(K_{t-1})\)，利用单向哈希链保证无法推导或重建历史密钥。  
  - 密钥保存在**硬件安全模块 (HSM)** 中，由受监管机构管理，防止服务提供方伪造或篡改时间戳。  

- **随机负载与双阶段编码机制**  
  - 每次生成时随机产生一个比特序列 \(R \in \{0,1\}^m\)，不与时间直接绑定、不永久存储。  
  - 通过**纠错编码 (ECC)** 扩展为更长的码字 \(P = ECC(R)\)，增强恢复鲁棒性（以 BCH(63,10) 码为例）。
  - 两阶段嵌入机制：  
    - **阶段 I**：种子由 \(PRF(K_t, R, h(\text{prefix}_i))\) 生成，用于前半段 token ；  
    - **阶段 II**：种子由 \(PRF(K_t, h(\text{prefix}_i))\) 生成，不含 R，用于后半段 token 。  
  - vocabulary 在每个位置被伪随机划分为 greenlist/redlist，通过调整 logits+δ 实现嵌入。

- **解码流程（司法验证）**  
  1. 针对候选时间窗口集合 \(T=\{t_1,t_2,\ldots,t_n\}\)，取出各时间密钥 \(K_t\)。  
  2. 利用 Stage II 的 token 重建 payload 并通过 ECC 解码恢复 \(R\)。  
  3. 使用 Stage I 验证 \(K_t\) 与 \(R\) 是否匹配，通过匹配率 Score(Kt,R) ≥ ϕ（阈值如 0.65）判定时间正确性。  

- **理论性质**  
  通过概率与编码理论分析，验证误判率接近 10⁻⁴⁴（false rejection）和 10⁻⁸（false acceptance），实现**司法级可靠性**。

---

## 三、实验设计

- **实验模型与场景**：  
  使用公开语言模型 **Qwen2.5-7B** 进行文本生成；设定 20 个高价值场景的提示（如创意撰写、报告生成）。

- **实验流程**：  
  每轮随机采样一个 prompt，生成两份文本（有水印 / 无水印），共 **800 组试验**。  
  每次生成时水印随机嵌入 10 位随机序列，经 BCH(63,10) 扩展后共嵌入 63 位负载。

- **Benchmark 与对比**：  
  本研究未与传统方案（Kirchenbauer et al. 2023, Yoo et al. 2024 等）直接量化比较，而是重点验证自身的**时间恢复准确性和防伪能力**。

---

## 四、资源与算力

- 文中未详细说明使用的 GPU 型号、数量或计算环境。
- 从描述看实验规模较轻（使用 Qwen2.5-7B 推理生成），推测未涉及训练，仅进行**生成与验证测试**。

---

## 五、实验数量与充分性

- **实验数量**：  
  共进行了 **800 次有水印与无水印文本生成实验**，每次验证 5 个候选时间密钥。

- **实验充分性**：  
  虽然未覆盖多模型或多语言场景，但设计包含批量试验与理论分析支撑，验证了检测的稳定性与准确性。

- **公平性与客观性**：  
  对照组（无水印文本）零误判率（0/800），验证客观，结果可信。

---

## 六、主要结论与发现

- TimeMark 框架可：
  - 实现 **100% 准确的生成时间恢复**；
  - 抵御提供者伪造、用户统计攻击；
  - 消除传统水印的可检测统计模式；
  - 满足司法电子证据的五项要求（真实性、可靠过程、完整性、可验证性、常规生成性）。

- 实验结果：  
  - 正确识别率：100%  
  - 无水印文本误识别率：0%  
  - 平均验证分数：水印文本 0.7699，对照文本 0.4993。

---

## 七、方法亮点

- **创新点**：  
  - 提出“可信水印”新范式，从概率检测跃升至确定性验证。  
  - 将时间信息从负载中解耦，通过时间相关密钥 + 随机负载机制防止伪造。  
  - 结合硬件安全模块（HSM）与密码哈希链，提供监管下的司法级防伪。  

- **工程与理论结合**：  
  - 双阶段编码使负载数据可验证；  
  - BCH 纠错编码提高可靠性；  
  - 理论概率分析与实验结果一致，证明系统稳定。

---

## 八、不足与局限

- **应用范围限制**：  
  目前仅适合**长文本生成**（约 ≥945 token），短文本可能无法嵌入足够水印信息。

- **健壮性假设**：  
  框架假定用户保留原始输出，尚未充分验证在**人工编辑或 paraphrasing 后的鲁棒性**。

- **实验覆盖不足**：  
  无跨模型或跨语言对比（只测试单一模型及英文场景）。

- **实现复杂性**：  
  强依赖外部硬件安全模块与监管密钥管理，部署成本较高。

---

（完）
