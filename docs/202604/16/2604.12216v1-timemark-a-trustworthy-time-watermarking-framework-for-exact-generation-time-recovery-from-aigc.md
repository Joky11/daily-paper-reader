---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：一种用于从AIGC精确恢复生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 9.0
evidence: 提出了一种用于AIGC的可信水印框架，用于验证生成时间并保护知识产权。
tldr: 本文提出TimeMark框架，为AIGC生成内容提供可信的时间水印，结合加密与时间依赖密钥确保生成时间可精确恢复，解决了传统水印的统计不确定性与伪造问题，在理论与实验上实现可靠性与可验证性，为AI内容知识产权争议提供司法级解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有文本水印依赖统计特征导致检测不确定、易被伪造，无法满足司法证据的可靠性需求。
method: 通过加密技术和时间依赖密钥生成机制，将时间信息嵌入无统计特征的随机比特序列，并结合双阶段编码与纠错码实现准确解码。
result: "TimeMark在实验中实现了100%生成时间识别准确率，并有效抵御统计分析和伪造攻击。"
conclusion: TimeMark框架在理论与实验上均实现了可作为司法证据的高可靠水印恢复，具备抗伪造和抗攻击能力。
---

## 摘要
大语言模型（LLMs）在文本生成中的广泛使用引发了日益增长的知识产权纠纷担忧。水印技术通过将元信息嵌入AI生成内容（AIGC）中，有潜力作为司法证据。然而，现有方法依赖于token分布中的统计信号，导致检测本质上具有概率性，在多比特编码（如时间戳）中可靠性下降。此外，此类方法会引入可检测的统计模式，使其易受伪造攻击，并使模型提供者能够伪造任意水印。为了解决这些问题，我们提出了可信水印的概念，它能够以100%的识别精度实现可靠恢复，同时抵御用户端的统计攻击和提供者端的伪造。我们重点研究用于司法证据的可信时间水印。我们的框架融合了密码学技术，在监管监督下将时间信息编码进时间相关的秘密密钥中，从而防止任意时间戳的伪造。水印载荷与时间解耦，为每个实例生成随机的、非存储的比特序列，从而消除统计模式。为确保可验证性，我们设计了一个双阶段编码机制，并结合纠错码，使生成时间的可靠恢复在理论上达到完美精度。理论分析和实验结果表明，我们的框架满足司法证据的可靠性要求，并为未来AIGC相关的知识产权纠纷提供了一个切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# TimeMark：用于AIGC生成时间精确恢复的可信时间水印框架 — 深度总结

---

## 一、核心问题与研究背景

- **研究动机**：  
  随着大型语言模型（LLMs）广泛用于生成高价值内容（如报告、创意文稿等），AI生成内容（AIGC）在知识产权领域引发越来越多的**归属与时间争议**。  
  法律上“生成时间”往往决定了作品的版权归属与优先创造权，然而现有AIGC缺乏可靠的生成时间记录。

- **现有方法缺陷**：  
  传统LLM水印多基于**统计偏置（token分布）**实现信号嵌入，存在三个关键问题：  
  1. 检测结果具有统计概率性质，无法达到司法所需的确定性。  
  2. 嵌入多比特信息（如时间戳）时准确率急剧下降。  
  3. 模型提供者可伪造任意水印或时间戳；同时，用户可通过统计攻击伪造水印信号。  

- **研究目标**：  
  构建一种可在司法场景中作为可靠证据的**可信时间水印（Trustworthy Time Watermark）**，满足：
  - 生成时间可恢复且100%准确；  
  - 模型提供者无法伪造；  
  - 用户无法通过统计学习冒充。

---

## 二、方法论与技术框架

### 2.1 总体思路

TimeMark框架以**“时间解耦 + 加密绑定 + 双阶段编码”**为核心策略：
- 不直接将时间戳嵌入到水印载荷；
- 使用时间演化密钥链（One-way Key Evolution）将时间关联信息绑定到加密密钥中；
- 水印载荷本身为随机且非持久比特序列，避免统计模式。

### 2.2 核心算法流程（文字说明）

1. **时间密钥生成**  
   - 以时间窗口为单位（如1分钟），定义时间密钥：  
     `Kt = H(Kt−1)` （H为单向哈希函数）  
   - 历史密钥仅存储在受监管的**硬件安全模块（HSM）**中，模型提供者无法访问，从而防止造假或回溯生成。

2. **随机载荷生成与编码**  
   - 每次生成文本时产生随机比特序列 `R ∈ {0,1}^m`；
   - 通过纠错编码（BCH码）扩展为冗余码字：  
     `P = ECC(R)`；增强恢复鲁棒性。

3. **双阶段编入策略**  
   - **阶段Ⅰ**（前半部分token）：使用 `PRF(Kt, R, h(prefix))` 生成种子，构造“绿 / 红列表”分词偏置；
   - **阶段Ⅱ**（后半部分token）：使用不含R的 `PRF(Kt, h(prefix))` 重建偏置；
   - 两阶段结合确保载荷可验证（第一阶段为验证位置，第二阶段为信息恢复位置）。

4. **解码与验证机制**
   - **Step 1：随机序列恢复**  
     从阶段Ⅱ的token偏置中恢复码字P并经纠错解码得到 `R`；
   - **Step 2：验证时间密钥**  
     使用同样的时间密钥Kt和恢复的R，重建阶段Ⅰ偏置，计算匹配率：  
     `Score(Kt, R) = (匹配token数) / (总token数)` >= 阈值ϕ（0.65即有效）；  
     若匹配超过阈值，则确认该时间窗口为实际生成时间。

5. **安全特性**  
   - 通过一方向密钥演化防止提供者伪造旧时间；
   - 随机化载荷消除统计特征，抵御用户伪造；
   - BCH纠错 + 双阶段验证确保100%可恢复性。

---

## 三、实验设计

- **实验场景**：  
  模拟AIGC生成用于知识产权判定场景，生成文本长度≥945 tokens。

- **模型与数据**：  
  使用 **Qwen2.5-7B** 作为实验LLM。  
  构建包含 20 个用于创意和报告生成的提示池，每次随机选取一个提示生成两种文本：
  - 带TimeMark水印；
  - 无水印（自然生成）。

- **实验规模**：  
  共进行了 **800次试验**。  
  对每一试验，考虑真实时间密钥及相邻4个候选密钥（共5个），验证恢复准确性。

- **对比设计**：  
  与无水印文本对比；不直接与其他论文方法进行量化对比，但理论上参照已有“多比特水印”和“统计攻击”思路构建评估。

---

## 四、资源与算力

- 论文未明确声明硬件算力或训练时长。  
  仅说明模型为Qwen2.5-7B，运行环境为普通文本生成实验，无额外训练过程。  
  可推断实验具有**轻算力需求**（推理级别）。

---

## 五、实验数量与充分性

- **实验数量**：800组生成与恢复实验。  
- **验证充分性**：
  - 对每组文本进行了多密钥验证（5候选窗口），确保区分能力；
  - 结合理论化推导（二项分布概率）展示恢复成功率接近1（误检概率约1e−44）。  
- **客观性**：
  - 所有文本使用相同模型与采样参数；
  - 无其他超参调优或手动干预；
  - 尽管缺少不同模型对比，但验证策略科学、公平。

---

## 六、主要结论与发现

- 实验结果表明：
  - **时间恢复准确率**：100%（所有800条水印文本均正确识别生成时间）；  
  - **误判率**：0%（无水印文本均未被识别为有时间水印）；  
  - **平均验证分值**：水印文本0.7699 vs 无水印文本0.4993，明显区分。  

- **理论验证**：  
  BCH码纠错能力确保在损失≤13比特情况下仍完全恢复，统计分析显示误差和伪验概率几乎为零，证明方案在数学上可达司法标准。

---

## 七、方法优点与创新点

- **司法级可信性**：
  将时间与密钥绑定而非直接编码，防止任意伪造，符合电子证据真实性要求。
- **100%恢复性能**：
  理论和实证均实现完美生成时间识别。
- **抗攻击能力强**：
  - 随机载荷消除统计模式；
  - 双阶段验证提高抗伪造性能；
  - 确保用户与服务商均无法欺骗验证系统。
- **技术整合创新**：
  融合加密学（HSM密钥链）、纠错码（BCH）、伪随机函数（PRF），形成跨领域的系统性解决方案。

---

## 八、不足与局限性

- **文本长度限制**：  
  当前要求较长生成序列（≥945 tokens）以维持稳定识别，对短文本不适用。
- **脆弱性假设**：  
  依赖用户保留原始AIGC版本；若编辑或改写，水印可能部分丢失。
- **实验覆盖**：  
  仅验证单模型（Qwen2.5-7B），未包含其他平台如GPT、Claude的通用性测试。
- **司法应用延伸**：  
  需进一步研究跨模型兼容性、司法验证流程与隐私管理。

---

（完）
