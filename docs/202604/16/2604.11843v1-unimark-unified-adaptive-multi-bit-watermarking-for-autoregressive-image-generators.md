---
title: "UniMark: Unified Adaptive Multi-bit Watermarking for Autoregressive Image Generators"
title_zh: UniMark：面向自回归图像生成器的统一自适应多比特水印
authors: "Yigit Yilmaz, Elena Petrova, Mehmet Kaya, Lucia Rossi, Amir Rahman"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.11843v1"
tags: ["query:q3"]
score: 10.0
evidence: 用于自回归图像生成器的水印技术，以保护图像所有权
tldr: 本文提出了UniMark，一种面向自回归图像生成器的统一自适应多比特隐形水印框架，解决了现有方法只能嵌入零比特水印、安全性弱及不可通用的问题。该方法无需训练，通过自适应语义分组、多比特分块编码和统一替换接口，实现高质量图像生成与多比特信息嵌入，具有优异的检测准确率和稳健性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11843v1/fig-001.webp\", \"caption\": \"Figure 4: Security analysis: (a) green ratio distributions under correct/wrong keys; (b) forgery success rate vs. partition exposure fraction.\", \"page\": 11, \"index\": 1, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-002.webp\", \"caption\": \"Figure 5: Efficiency analysis: (a) absolute time comparison (log scale); (b) relative overhead percentage.\", \"page\": 11, \"index\": 2, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-003.webp\", \"caption\": \"Figure 2: Radar chart comparing robustness (TPR@FPR=1%) across six attack types on LlamaGen.\", \"page\": 10, \"index\": 3, \"width\": 392, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-004.webp\", \"caption\": \"Figure 3: Parameter sensitivity analysis: effect of green ratio γ on (a) FID, (b) TPR@FPR=1%, and (c) 32-bit extraction accuracy.\", \"page\": 10, \"index\": 4, \"width\": 822, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-005.webp\", \"caption\": \"Table 1: Image quality comparison across AR models. ∆FID denotes the FID increase compared to unwatermarked generation.\", \"page\": 8, \"index\": 5, \"width\": 636, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-006.webp\", \"caption\": \"Figure 6: Attack strength analysis: (a) JPEG quality, (b) Gaussian noise σ, (c) crop ratio vs. detection and extraction performance.\", \"page\": 12, \"index\": 6, \"width\": 821, \"height\": 220}]"
motivation: 为提升自回归图像生成的版权保护和水印鲁棒性，克服现有水印方法在信息容量、安全性与通用性上的局限。
method: 方法包括自适应语义分组、分块多比特编码及统一令牌替换接口。
result: UniMark在三种自回归模型上达到了当前最先进的图像质量和水印检测准确率，并对多种破坏攻击保持鲁棒。
conclusion: UniMark在无需训练的前提下实现了通用、高效和安全的多比特隐形水印嵌入，为AI图像的版权保护提供了新方向。
---

## 摘要
针对自回归（AR）图像生成的隐形水印近年来受到关注，被视为保护图像所有权和追踪 AI 生成内容的一种手段。然而，现有方法存在以下三方面局限：（1）仅嵌入用于二元验证的零比特水印，无法传递多比特信息；（2）依赖静态的码本划分策略，一旦划分方案泄露便容易受到安全攻击；（3）只能针对特定的 AR 架构设计，无法在不同的 AR 范式之间泛化。我们提出 \method{}，一种无需训练的自回归图像生成器统一水印框架，克服上述三大限制。\method{} 包含三个核心组件：\textbf{自适应语义分组（Adaptive Semantic Grouping, ASG)}，基于语义相似性和密钥动态划分码本条目，从而保证图像质量与安全性；\textbf{分块多比特编码（Block-wise Multi-bit Encoding, BME)}，将 token 序列划分为不同区块，并通过纠错码在各区块间编码不同比特，实现可靠的信息传输；以及\textbf{统一的 Token 替换接口（Unified Token-Replacement Interface, UTRI)}，抽象水印嵌入过程，用以支持下一 token 预测（如 LlamaGen）和下一尺度预测（如 VAR）两种范式。我们提供关于检测错误率与嵌入容量的理论分析。在三个 AR 模型上的大量实验表明，\method{} 在图像质量（FID）、水印检测精度和多比特信息提取方面均达到了当前最佳性能，同时对裁剪、JPEG 压缩、高斯噪声、模糊、颜色抖动以及随机擦除等攻击保持稳健性。

## Abstract
Invisible watermarking for autoregressive (AR) image generation has recently gained attention as a means of protecting image ownership and tracing AI-generated content. However, existing approaches suffer from three key limitations: (1) they embed only zero-bit watermarks for binary verification, lacking the ability to convey multi-bit messages; (2) they rely on static codebook partitioning strategies that are vulnerable to security attacks once the partition is exposed; and (3) they are designed for specific AR architectures, failing to generalize across diverse AR paradigms. We propose \method{}, a training-free, unified watermarking framework for autoregressive image generators that addresses all three limitations. \method{} introduces three core components: \textbf{Adaptive Semantic Grouping (ASG)}, which dynamically partitions codebook entries based on semantic similarity and a secret key, ensuring both image quality preservation and security; \textbf{Block-wise Multi-bit Encoding (BME)}, which divides the token sequence into blocks and encodes different bits across blocks with error-correcting codes for reliable message transmission; and \textbf{a Unified Token-Replacement Interface (UTRI)} that abstracts the watermark embedding process to support both next-token prediction (e.g., LlamaGen) and next-scale prediction (e.g., VAR) paradigms. We provide theoretical analysis on detection error rates and embedding capacity. Extensive experiments on three AR models demonstrate that \method{} achieves state-of-the-art performance in image quality (FID), watermark detection accuracy, and multi-bit message extraction, while maintaining robustness against cropping, JPEG compression, Gaussian noise, blur, color jitter, and random erasing attacks.

---

## 论文详细总结（自动生成）

# UniMark：面向自回归图像生成器的统一自适应多比特水印框架 — 论文详细总结

---

## 一、核心问题与研究动机

- **研究背景**  
  随着自回归（Autoregressive, AR）图像生成模型（如 LlamaGen、VAR、MAGVIT 系列）的快速发展，AI 生成内容（AIGC）的版权保护和溯源需求愈发紧迫。与扩散模型相比，AR 模型在生成质量与控制精度上已取得竞争性成果，但其**隐形水印技术**研究仍处于早期阶段。

- **现有方法的三大局限**：
  1. **仅支持零比特嵌入**：只能判断图像是否包含水印，无法携带用户标识、时间戳等多比特信息。
  2. **安全性不足**：常见的静态“红-绿”码本划分易被逆向推测，从而导致伪造或篡改。
  3. **架构特定性强**：针对不同AR范式（如next-token与next-scale）需采用不同嵌入逻辑，缺乏统一性。

- **研究目标**  
  论文旨在提出一种**无需额外训练、架构无关、可多比特嵌入且具鲁棒性**的隐形水印框架，以在保持图像质量的同时增强安全性与可扩展性。

---

## 二、方法论概述

UniMark 框架由三大核心组件构成，形成一个完整的**“训练自由 + 语义自适应 + 多比特嵌入 + 跨架构兼容”**体系。

### 1. 自适应语义分组（Adaptive Semantic Grouping, ASG）

- **核心思想**：利用**语义相似性矩阵**与**加密密钥 κ**对码本进行**动态、位置相关的分组**。
- **主要流程**：
  - 计算码本中每个 token 嵌入向量之间的余弦相似度矩阵 \( s_{ij} = \frac{e_i^\top e_j}{\|e_i\|\|e_j\|} \)。
  - 对每个位置 \( i \)，生成密钥相关的随机种子 \( \text{seed}_i = H(\kappa \| i) \)，并据此打乱码本索引。
  - 将码本分为绿色（允许替换）与红色（不替换）集合 \( G_i, R_i \)，比例由 γ 控制（默认 0.5）。
  - 若当前生成 token 不属于绿色集合，则以相似度最大原则替换为绿色候选。
- **优点**：每个位置具有独立分组，攻击者无法从单个样本推断全局模式，提升安全性与隐匿性。

### 2. 分块多比特编码（Block-wise Multi-bit Encoding, BME）

- **设计目的**：支持多比特信息的嵌入与可靠恢复。
- **编码机制**：
  - 将消息向量 \( m \in \{0,1\}^B \) 经 BCH 纠错码转为 \( m′ \in \{0,1\}^n \)。
  - 将 token 序列按 n 块划分，每块对应一个比特。
  - 若当前比特为 1，使用绿色集合；若为 0，使用红色集合。
  - 提取时计算每块中绿色 token 的比例 \( r_j = \frac{1}{|B_j|}\sum 1[t_i \in G_i] \)，通过阈值 \(τ=0.5\) 恢复比特，再经 BCH 解码恢复原消息。
- **零比特检测**：特例为全局绿色比例检验，基于显著性检验（z-test）判定存在性。
- **理论分析**：给出了误检率（False Positive Rate）与最大嵌入容量的数学上界推导。

### 3. 统一 Token 替换接口（Unified Token-Replacement Interface, UTRI）

- **目标**：适配不同自回归模型范式（next-token 与 next-scale）。
- **三步抽象接口**：
  1. **Get Token Sequence:** 获取原始 token 序列；
  2. **Replace Tokens:** 依据 ASG 与 BME 执行 token 替换；
  3. **Decode Image:** 将替换后序列重构为图像。
- **兼容性设计**：  
  - 对 VAR 等多尺度生成器，合并不同尺度 token 序列，块划分保持尺度一致性。

---

## 三、实验设计

- **模型与数据源**：  
  在三个具有代表性的AR模型上进行验证：
  1. **LlamaGen-XL**（next-token范式, K=16384, N=256）  
  2. **VAR-d20**（next-scale范式, 10层尺度, K=4096, N=680）  
  3. **Open-MAGVIT2**（开源、极大码本 K=262144, N=256）

  图像生成基于 ImageNet 验证集共 5000 张样本。

- **对比方法**：
  1. **IndexMark (2025)**：静态红绿划分训练自由方案（零比特）。  
  2. **AR-Watermark (Jovanović, 2025)**：LLM 式偏置水印。  
  3. **Tree-Ring (2023)**：扩散模型水印基线，作横向比较。

- **评估指标**：
  - **图像质量**：FID、PSNR、SSIM  
  - **水印检测**：TPR@FPR=1%  
  - **多比特信息提取精度**：Bit Accuracy  
  - **鲁棒性攻击测试**：JPEG压缩、加噪、模糊、裁剪、颜色扰动、随机擦除等。

- **实验参数**：
  - γ=0.5，消息长度B=32比特；
  - 使用 BCH(63,36,5) 纠错码；
  - 显著性水平 α=0.01。

---

## 四、资源与算力

- 文中明确指出该方法为**训练自由（training-free）**，嵌入过程仅在生成过程中执行，无额外训练开销。
- 计算开销：
  - 每张图像增加约 8–15 毫秒，开销占比仅 **1.2%–1.9%**；
  - 主要耗时来源为语义相似度查找（单次矩阵向量操作）。
- **未明确描述 GPU 型号、训练时间或硬件规模**，但文中强调可直接部署于任意生成模型的推理流程中。

---

## 五、实验数量与充分性

- **主要实验规模**：
  - 三种AR模型 × 多个攻击场景 × 多种参数设置（γ、消息长度）。
  - 消融实验包括去除ASG、BME、UTRI或纠错码模块。
  - 参数敏感性（γ取0.3–0.7范围）、容量分析、攻击强度曲线、码本规模影响。
  - 安全性实验展示在部分密钥暴露下的伪造成功率。

- **充分性评价**：
  - 覆盖了性能、鲁棒性、安全性、效率与可扩展性；
  - 对比方法包含从AR到扩散体系；
  - 实验数据量充分且客观，结论较有说服力。

---

## 六、主要结论与发现

1. **图像质量**：FID 提升极小（≤0.1），质量几乎无损。
2. **检测性能**：TPR@FPR=1% 达 99.8% 以上。
3. **多比特提取**：32位消息准确率 >99%，64位仍保持>95%。
4. **鲁棒性**：JPEG质量50%、裁剪75%面积下仍可识别，表现领先。
5. **安全性**：密钥相关分组有效防止伪造，错误密钥下无差异信号。
6. **效率**：嵌入开销 <2%，可实时集成。
7. **容量趋势**：长token序列模型（VAR）具更高信息嵌入能力。

---

## 七、优点与创新点

- ✅ **方法创新**：引入语义感知 + 密钥依赖的动态码本划分机制，兼顾保真度与安全性。  
- ✅ **普适性高**：统一接口适配多种AR架构，无需改动模型。  
- ✅ **信息容量提升**：BME + 纠错码设计实现实用多比特嵌入。  
- ✅ **理论严谨**：推导误检率与嵌入容量上限，提供可解释性指标。  
- ✅ **实验全面**：涵盖多模型、多攻击、多参数及消融验证。  
- ✅ **部署轻量**：无训练、低延迟，实用性强。

---

## 八、不足与局限

- ⚠️ **容量受限**：嵌入容量随 token 序列长度受限，仍无法支持高信息量场景。  
- ⚠️ **鲁棒性边界**：在强噪声或大幅裁剪（<60%保留）下性能明显下降。  
- ⚠️ **未涉及对抗攻击或模型再生成攻击（Regeneration Attack）**的防御分析。  
- ⚠️ **未提供硬件细节**，实际部署性能可能因模型实现差异有所波动。  
- ⚠️ **仅测试 ImageNet 范围**，缺乏跨领域验证（如人像、医学图像等）。

---

**（完）**
