---
title: "UniMark: Unified Adaptive Multi-bit Watermarking for Autoregressive Image Generators"
title_zh: UniMark：面向自回归图像生成器的统一自适应多比特水印方法
authors: "Yigit Yilmaz, Elena Petrova, Mehmet Kaya, Lucia Rossi, Amir Rahman"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.11843v1"
tags: ["query:q8"]
score: 9.5
evidence: 针对自回归图像生成器的统一多位水印技术，用于保护所有权和追踪内容。
tldr: 本文提出了UniMark，一个用于自回归图像生成器的统一自适应多比特水印框架。该方法无需训练，结合自适应语义分组、块级多比特编码和统一令牌替换接口，实现跨模型高质量水印嵌入与检测，并在多种攻击下保持良好稳健性与提取准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11843v1/fig-001.webp\", \"caption\": \"Figure 4: Security analysis: (a) green ratio distributions under correct/wrong keys; (b) forgery success rate vs. partition exposure fraction.\", \"page\": 11, \"index\": 1, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-002.webp\", \"caption\": \"Figure 5: Efficiency analysis: (a) absolute time comparison (log scale); (b) relative overhead percentage.\", \"page\": 11, \"index\": 2, \"width\": 818, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-003.webp\", \"caption\": \"Figure 2: Radar chart comparing robustness (TPR@FPR=1%) across six attack types on LlamaGen.\", \"page\": 10, \"index\": 3, \"width\": 392, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-004.webp\", \"caption\": \"Figure 3: Parameter sensitivity analysis: effect of green ratio γ on (a) FID, (b) TPR@FPR=1%, and (c) 32-bit extraction accuracy.\", \"page\": 10, \"index\": 4, \"width\": 822, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-005.webp\", \"caption\": \"Table 1: Image quality comparison across AR models. ∆FID denotes the FID increase compared to unwatermarked generation.\", \"page\": 8, \"index\": 5, \"width\": 636, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.11843v1/fig-006.webp\", \"caption\": \"Figure 6: Attack strength analysis: (a) JPEG quality, (b) Gaussian noise σ, (c) crop ratio vs. detection and extraction performance.\", \"page\": 12, \"index\": 6, \"width\": 821, \"height\": 220}]"
motivation: 现有自回归图像生成水印方法存在信息容量低、安全性差、缺乏跨模型通用性的限制。
method: UniMark通过引入自适应语义分组、块级多比特编码以及统一的令牌替换接口，实现了无训练的水印嵌入与检测。
result: 实验表明UniMark在图像质量、水印检测准确率和多比特信息提取方面均达到最先进水平，并对多种攻击具备鲁棒性。
conclusion: UniMark在多个自回归图像生成模型中实现了高质量图像生成和稳健的多比特水印识别，显著提升了安全性与通用性。
---

## 摘要
针对自回归（Autoregressive，AR）图像生成的隐形水印技术近年来受到广泛关注，因为它可以用于保护图像所有权并追踪人工智能生成内容。然而，现有方法存在三项主要局限：（1）仅嵌入用于二元验证的零比特水印，无法传递多比特信息；（2）依赖静态的代码簿划分策略，一旦划分方式被泄露便易受安全攻击；（3）仅适用于特定的 AR 架构，缺乏对不同 AR 范式的泛化能力。本文提出了一个无需训练的统一水印框架 \method{}，用于自回归图像生成器，以同时克服上述三项局限。\method{} 包含三个核心模块：\textbf{自适应语义分组（Adaptive Semantic Grouping, ASG)}——基于语义相似度和密钥动态划分代码簿条目，以同时保证图像质量与安全性；\textbf{分块多比特编码（Block-wise Multi-bit Encoding, BME)}——将 token 序列划分为若干块，并在不同块中利用纠错编码传输多比特消息，以确保信息传递的可靠性；以及\textbf{统一 Token 替换接口（Unified Token-Replacement Interface, UTRI)}——对水印嵌入过程进行抽象，兼容“下一 token 预测”（如 LlamaGen）与“下一尺度预测”（如 VAR）两类范式。本文从理论上分析了检测误差率与嵌入容量，并在三个 AR 模型上进行了大量实验，结果表明 \method{} 在图像质量（FID）、水印检测准确率及多比特消息提取性能上均达到当前最优效果，同时对裁剪、JPEG 压缩、高斯噪声、模糊、色彩抖动及随机擦除等攻击保持较强鲁棒性。

## Abstract
Invisible watermarking for autoregressive (AR) image generation has recently gained attention as a means of protecting image ownership and tracing AI-generated content. However, existing approaches suffer from three key limitations: (1) they embed only zero-bit watermarks for binary verification, lacking the ability to convey multi-bit messages; (2) they rely on static codebook partitioning strategies that are vulnerable to security attacks once the partition is exposed; and (3) they are designed for specific AR architectures, failing to generalize across diverse AR paradigms. We propose \method{}, a training-free, unified watermarking framework for autoregressive image generators that addresses all three limitations. \method{} introduces three core components: \textbf{Adaptive Semantic Grouping (ASG)}, which dynamically partitions codebook entries based on semantic similarity and a secret key, ensuring both image quality preservation and security; \textbf{Block-wise Multi-bit Encoding (BME)}, which divides the token sequence into blocks and encodes different bits across blocks with error-correcting codes for reliable message transmission; and \textbf{a Unified Token-Replacement Interface (UTRI)} that abstracts the watermark embedding process to support both next-token prediction (e.g., LlamaGen) and next-scale prediction (e.g., VAR) paradigms. We provide theoretical analysis on detection error rates and embedding capacity. Extensive experiments on three AR models demonstrate that \method{} achieves state-of-the-art performance in image quality (FID), watermark detection accuracy, and multi-bit message extraction, while maintaining robustness against cropping, JPEG compression, Gaussian noise, blur, color jitter, and random erasing attacks.

---

## 论文详细总结（自动生成）

# UniMark：面向自回归图像生成器的统一自适应多比特水印方法 — 中文总结

---

## 一、研究背景与核心问题

- **研究动机**  
  自回归（Autoregressive, AR）图像生成模型（如 LlamaGen、VAR、Open-MAGVIT2）近年来在生成质量上已接近或超过扩散模型，但由此也带来了版权与内容溯源问题。  
  传统“隐形水印”方法可用于验证 AI 生成内容的所有权或来源，但现有针对 AR 模型的方案存在以下缺陷：
  1. **信息承载能力不足**：多数方法仅实现“零比特”验证（仅判断是否带水印），无法嵌入多比特信息（如用户 ID、时间戳等）。  
  2. **易受攻击**：静态的代码簿（codebook）划分方式一旦被暴露，攻击者可轻易去除或伪造水印。  
  3. **缺乏通用性**：多数方法依赖特定架构（如仅适配 next-token 或 next-scale 模型），无法跨范式应用。  

- **研究目标**  
  提出一种**统一、无训练、自适应的多比特水印框架**——**UniMark**，同时满足高质量生成、信息可恢复、跨架构通用与安全防护需求。

---

## 二、方法论与算法框架

UniMark 包含 **三大核心模块**，以训练无关、令牌级（token-level）的嵌入方式实现稳健水印。

### 1. 自适应语义分组（Adaptive Semantic Grouping, ASG）

- **思想**：利用代码簿中各 token 的语义相似性，在保证视觉质量的前提下实现密钥依赖的动态划分。  
- **关键机制**：  
  - 基于 codebook embedding 计算余弦相似度矩阵 \( s_{ij} \)。  
  - 使用加密哈希函数 \( H(\kappa‖i) \) 生成位置特定的随机种子与伪随机排列。  
  - 按“绿色组 (Gi)”与“红色组 (Ri)”划分 codebook，比例参数为 \( γ \)（典型为 0.5）。  
  - 若生成 token 不在当前绿色组，则替换为与其语义最相近的绿色 token。  

→ 该策略保证了安全密钥唯一性，每个 token 位置的分组均不同，难以被逆向推测。

### 2. 分块多比特编码（Block-wise Multi-bit Encoding, BME）

- **目标**：允许在生成序列中嵌入任意多比特信息。
- **流程描述**：  
  1. 将消息 \( m ∈ {0,1}^B \) 经 BCH(n, B, d) 编码得到纠错码 \( m' \)。  
  2. 将 token 序列划分为 n 个块 \( B_j \)，每个块嵌入一个编码比特。  
  3. 当 \( m'_j = 1 \) 时使用绿色组，当 \( m'_j = 0 \) 时使用红色组。  
  4. 通过统计每块中绿色 token 的比例 \( r_j \)，并以阈值 \( τ=0.5 \) 判定比特值。  
  5. BCH 解码恢复原始消息，实现多比特提取与错误纠正。  

- **理论分析**：作者给出了检测误检率（False Positive Rate）与最大信息容量公式，证明嵌入容量与 token 数量 \( N \) 成正比。

### 3. 统一 Token 替换接口（Unified Token-Replacement Interface, UTRI）

- **设计目的**：兼容不同的自回归预测方式。  
  - 对 **next-token 模型**（如 LlamaGen）直接按生成顺序处理 token。  
  - 对 **next-scale 模型**（如 VAR）按照尺度顺序串接 token，并保持块划分与尺度一致。  
- 该接口使 UniMark 能够以统一流程应用于不同架构而无需模型改动。

---

## 三、实验设计

### 1. 模型与数据场景

- **评测模型**：
  - LlamaGen-XL：next-token 模型，codebook \( K=16384 \)，\( N=256 \)。  
  - VAR-d20：next-scale 模型，codebook \( K=4096 \)，\( N=680 \)。  
  - Open-MAGVIT2：next-token 模型，codebook \( K=262144 \)，\( N=256 \)。  
- **数据集**：ImageNet 验证集（生成 5000 张图像）。  
- **水印设置**：默认传输 32 比特信息，BCH(63, 36, 5) 纠错码，显著性水平 α=0.01。

### 2. 对比基线

- **IndexMark**（Tong 2025）：训练无关、零比特静态分区法。  
- **AR-Watermark**（Jovanović 2025）：基于 LLM red-green 分组的图像版本。  
- **Tree-Ring Watermark**（Wen 2023）：扩散模型水印（作为跨范式基线）。

### 3. 评测指标

| 指标 | 含义 |
|------|------|
| FID ↓ | 图像质量 |
| PSNR / SSIM ↑ | 图像一致性 |
| TPR@FPR=1% | 零比特检测性能 |
| Bit Accuracy ↑ | 多比特信息提取正确率 |
| 鲁棒性测试 | JPEG 压缩、噪声、模糊、裁剪、颜色抖动、随机擦除 |

---

## 四、资源与算力信息

- **论文中未明确说明**训练或推理硬件配置。  
- UniMark 属于**无训练框架**，仅在生成阶段执行 token 替换推理，计算开销极小（额外时延 <2%）。  
- 在三个模型的推理实验中，单图像增加 **8–15 ms** 时间。

---

## 五、实验数量与充分性

作者共进行了**多层次的实验证明**：

1. **主实验（3个模型 × 各指标）** → 图像质量 / 检测 / 多比特提取性能  
2. **鲁棒性实验（6种攻击 × 各模型）**  
3. **参数敏感性研究**：绿比率 γ 从 0.3–0.7  
4. **消融实验（去除 ASG / BME / 纠错编码 / UTRI）**  
5. **理论验证实验**：信息容量与 codebook 大小  
6. **效率与安全分析**：伪造概率、钥匙错误检测分布  

→ 实验覆盖全面，结论一致，指标间具有显著优势，体现了充分性与公平性。

---

## 六、主要结论与发现

- **高质量生成**：FID 提升最小，仅 +0.07（LlamaGen），优于静态划分方法。  
- **精准检测**：TPR@FPR=1% 可达 99.8–99.9%。  
- **可靠多比特提取**：32 位信息恢复准确率 >99%；长达 64 位时仍保持约 95% 准确率。  
- **强鲁棒性**：在 JPEG、噪声、裁剪等强攻击下仍保持 > 90% 检测率。  
- **高安全性**：密钥错误时检测结果近似未加水印图像；伪造成功率在静态划分下 68%，而 UniMark < 2%。  
- **高效率**：平均生成时间开销 ≈ 1.2–1.9%，可实时部署。  

---

## 七、方法优点与亮点

1. **跨模型统一性**：UTRI 接口适配各种自回归生成架构。  
2. **自适应安全性**：ASG 结合语义相似度与密钥随机性，抵抗逆向分析与伪造。  
3. **支持多比特水印**：BME 及 BCH 纠错确保信息传递能力与可靠性。  
4. **理论与实验并重**：提供可证明的误检率与容量上界。  
5. **高效 & 无需训练**：仅在推理阶段执行，延迟低，占用极小。  
6. **系统性验证**：大量参数、鲁棒性与消融实验支持其有效性。

---

## 八、不足与局限

- **容量限制**：嵌入信息量受 token 数量与 codebook 规模约束，短序列模型下容量有限。  
- **未覆盖对抗性生成攻击**：如重生成、模型逆向等高级攻击场景仍需研究。  
- **缺少大规模用户实测**：未报告在大规模分布式生成或多任务环境的行为。  
- **仅限图像模型**：尚未扩展至视频或多模态AR生成任务。  

---

（完）
