---
title: "TimeMark: A Trustworthy Time Watermarking Framework for Exact Generation-Time Recovery from AIGC"
title_zh: TimeMark：用于精确恢复 AIGC 生成时间的可信时间水印框架
authors: "Shangkun Che, Silin Du, Ge Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12216v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于验证AIGC真实性和生成时间的水印技术
tldr: "本文提出TimeMark可靠时间水印框架，用于解决AI生成内容的知识产权与司法取证难题。通过结合密码学方法和时间相关密钥，在监管下嵌入不可伪造的时间信息，并使用两阶段编码与纠错机制实现100%准确恢复生成时间，为AIGC提供可信的证据。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12216v1/fig-001.webp\", \"caption\": \"Table 1 From legal evidentiary principles to practical requirements for AIGC timestamping mechanisms.\", \"page\": 7, \"index\": 1, \"width\": 977, \"height\": 579}]"
motivation: 现有文本生成水印方法检测不可靠且易被伪造，不能满足司法取证对可信度的要求。
method: 作者提出结合密码学的两阶段时间水印编码机制，并使用纠错码确保时间信息的可靠恢复。
result: 实验和理论分析表明，该框架可在司法领域实现生成时间的精确恢复，具有高可靠性和抗伪造能力。
conclusion: TimeMark框架实现了理论上的完美时间恢复，为未来AI生成内容的知识产权纠纷提供了可行的司法证据解决方案。
---

## 摘要
大型语言模型（LLMs）在文本生成中的广泛应用引发了关于知识产权纠纷的日益关注。水印技术通过将元信息嵌入人工智能生成内容（AIGC）中，有潜力作为司法证据使用。然而，现有方法依赖于标记分布中的统计信号，从而导致其检测结果具有固有的概率性，并在多比特编码（例如时间戳）中可靠性降低。此外，这类方法会引入可检测的统计模式，使其容易受到伪造攻击，并使模型提供者能够任意伪造水印。为解决这些问题，我们提出“可信水印”概念，它能在抵御用户侧的统计攻击和提供者侧的伪造攻击的同时，实现100%的识别精度与可靠的恢复。我们重点关注可作为司法证据的可信时间水印。该框架将密码学技术相结合，在监管监督下将时间信息编码为与时间相关的秘密密钥，防止任意时间戳伪造。水印载荷与时间解耦，并在每个实例中生成随机、无存储的比特序列，从而消除统计模式。为保证可验证性，我们设计了一种两阶段编码机制，并结合纠错码实现了理论上完美的生成时间恢复精度。理论分析与实验结果均表明，我们的框架满足司法证据的可靠性要求，为未来与 AIGC 相关的知识产权纠纷提供了切实可行的解决方案。

## Abstract
The widespread use of Large Language Models (LLMs) in text generation has raised increasing concerns about intellectual property disputes. Watermarking techniques, which embed meta information into AI-generated content (AIGC), have the potential to serve as judicial evidence. However, existing methods rely on statistical signals in token distributions, leading to inherently probabilistic detection and reduced reliability, especially in multi-bit encoding (e.g., timestamps). Moreover, such methods introduce detectable statistical patterns, making them vulnerable to forgery attacks and enabling model providers to fabricate arbitrary watermarks. To address these issues, we propose the concept of trustworthy watermark, which achieves reliable recovery with 100% identification accuracy while resisting both user-side statistical attacks and provider-side forgery. We focus on trustworthy time watermarking for use as judicial evidence. Our framework integrates cryptographic techniques and encodes time information into time-dependent secret keys under regulatory supervision, preventing arbitrary timestamp fabrication. The watermark payload is decoupled from time and generated as a random, non-stored bit sequence for each instance, eliminating statistical patterns. To ensure verifiability, we design a two-stage encoding mechanism, which, combined with error-correcting codes, enables reliable recovery of generation time with theoretically perfect accuracy. Both theoretical analysis and experiments demonstrate that our framework satisfies the reliability requirements for judicial evidence and offers a practical solution for future AIGC-related intellectual property disputes.

---

## 论文详细总结（自动生成）

# 《TimeMark：用于精确恢复 AIGC 生成时间的可信时间水印框架》详细总结

---

## 一、研究背景与核心问题

- **研究动机**：  
  随着大型语言模型（LLM）在文本生成中的广泛应用，越来越多的创作性与商业内容源自 AIGC（AI-Generated Content）。这导致内容原创性、知识产权与司法取证中的“生成时间”成为关键问题。  
  当前的 LLM 水印技术主要用于识别 AI 生成内容，但存在以下不足：  
  1. 检测依赖统计信号，结果具有概率性，无法实现 100% 准确恢复；  
  2. 多比特信息嵌入（如时间戳）会导致可靠性骤降；  
  3. 模型提供者可伪造任意时间水印，缺乏防伪机制；  
  4. 可检测的统计模式可被攻击者学习并伪造，破坏司法可信度。  

- **研究目标**：  
  作者提出“可信时间水印（Trustworthy Time Watermark）”的概念，旨在：
  - 精确恢复文本生成时间；
  - 防止模型提供者与用户双方的伪造；
  - 消除统计模式，抵御统计攻击；
  - 满足司法证据“真实性、可靠性、可验证性”的要求。  

---

## 二、方法论：TimeMark 框架设计

### 1. 核心思想

TimeMark 的关键创新是**将时间信息与水印载荷解耦**，通过密码学机制实现时间绑定与随机载荷生成，从而实现 100% 精确的时间恢复与防伪保证。其主要思想包括：

- **时间绑定密钥演化机制**：  
  生成时间 t 对应一个时间密钥 \( K_t = H(K_{t-1}) \)，其中 H 是不可逆哈希函数。  
  历史密钥仅存放于**受监管的硬件安全模块（HSM）**中，服务提供商无法回溯或伪造过去的时间密钥。

- **随机水印载荷生成**：  
  每次生成时创建一个随机比特序列 \( R \in \{0,1\}^m \)，并通过纠错编码（如 BCH 编码）得到扩展载荷 \( P = ECC(R) \)。  
  随机载荷消除了可识别模式，防止统计伪造。

- **两阶段编码策略**：  
  - **第一阶段（Stage I）**：利用时间密钥 \( K_t \)、随机序列 \( R \) 和前缀哈希生成伪随机种子 \( S_i^1 \)，确定绿色词表（greenlist）并进行有偏采样。  
  - **第二阶段（Stage II）**：从生成的词序列后半部分移除 R，仅使用 \( K_t \) 生成种子 \( S_i^2 \)，确保在验证时可独立恢复。  

- **解码与验证**：  
  1. **解码步骤一**：利用候选时间密钥 \( K_t \) 重建 Stage II 的 greenlist，以多数投票恢复纠错载荷 \(\hat{P}\)，再解码出随机序列 \(\hat{R}\)。  
  2. **解码步骤二**：用候选 \( K_t \) 与 \(\hat{R}\) 重建 Stage I 的 greenlist，并计算匹配率  
     \[
     Score(K_t, \hat{R}) = \frac{1}{\alpha |P|} \sum_{i=1}^{\alpha |P|} Match(i)
     \]
     若大于阈值 \( \phi = 0.65 \)，则判定该时间密钥有效。

---

## 三、实验设计

- **实验场景与任务**：  
  - 20 条与创意写作和报告生成相关的提示（prompts）。  
  - 每轮实验生成两组文本（有水印 vs 无水印）。  
  - 共计 800 轮实验。

- **对比方式**：  
  - 验证能否从真实水印文本中恢复正确时间；  
  - 测试非水印文本是否会被误判。  
  （未引用外部基准数据集或与其他模型直接对比，重点是内部验证可靠性。）

- **模型与参数设置**：  
  - 使用 **Qwen2.5-7B** 模型生成文本；  
  - 随机序列长度：10 bits；  
  - BCH 编码长度：63 bits；  
  - 文本长度不少于 945 tokens。  

---

## 四、资源与算力

- 文中仅提及模型为 Qwen2.5-7B，但**未说明 GPU 型号、数量或计算资源**。  
- 未报告训练时长或推理时间，仅描述框架部署与验证阶段。

---

## 五、实验数量与充分性

- **实验规模**：  
  共 800 组对照实验（即 1600 篇文本）。  
  每组实验评估解码成功率与验证分数分布。  

- **结果表现**：  
  - 有水印文本的生成时间识别率：100%；  
  - 无水印文本误检率：0%；  
  - 平均验证得分：有水印 0.7699，无水印 0.4993。  

- **充分性评价**：  
  实验较为系统地涵盖时间识别与抗伪造性研究，但场景单一（仅生成任务），缺少跨领域与鲁棒性测试（如文本修改、不同模型等），因此在**司法实际场景**下仍需扩展验证。

---

## 六、主要结论与发现

- TimeMark 实现了理论与实验上**100% 的时间识别准确性**；  
- 通过 HSM 与时间密钥链设计，**杜绝模型提供者伪造过往时间戳**；  
- 随机载荷与纠错编码结合，**在防止统计攻击的同时保持精确恢复**；  
- 提供了一个**满足司法取证标准（真实性、可靠性、可验证性）**的 AIGC 技术基础。  

---

## 七、方法与研究优点

- 🔒 **司法级可信防伪机制**：时间密钥受监管控制，无法被服务方伪造；  
- 🧠 **独创的时间与载荷分离思想**：消除统计特征，防止用户侧攻击；  
- 🔁 **两阶段水印编码设计**：通过双阶段验证机制实现自动验证与冗余校正；  
- 🧩 **纠错编码（BCH）引入**：确保在少量噪声/解码误差下仍可精确恢复生成时间；  
- 🧾 **理论分析严密**：提供详细概率计算与误差上界证明，验证系统可靠性。  

---

## 八、不足与局限

- ⚙️ **文本长度限制**：  
  要保证近乎完美的恢复，需要生成文本长度 ≥945 个 token，对短文本应用受限。  

- 🧪 **鲁棒性不足**：  
  假设用户未编辑 AIGC 原文，缺乏在修改、 paraphrasing、翻译后的可验证性评估。  

- 📊 **实验范围单一**：  
  未包含跨语言、不同类型 LLM 或开放攻击场景（如重写攻击、逆向分析）的测试。  

- 🔍 **算力与效率指标缺失**：  
  未说明编码开销、检测时延或硬件部署成本。  

- 🧑‍⚖️ **司法落地性待验证**：  
  虽理论满足司法标准，但尚未与电子取证或区块链司法平台联测。  

---

**（完）**
